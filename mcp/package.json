{
  "name": "custard-calendar-mcp",
  "version": "1.0.0",
  "description": "MCP server for Custard Calendar — query Flavor of the Day data via AI assistants",
  "type": "module",
  "bin": {
    "custard-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
