---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - custard-calendar/docs/index.html
  - custard-calendar/docs/calendar.html
  - custard-calendar/docs/map.html
  - custard-calendar/docs/radar.html
  - custard-calendar/docs/alerts.html
  - custard-calendar/docs/siri.html
  - custard-calendar/docs/forecast-map.html
  - custard-calendar/docs/quiz.html
  - custard-calendar/docs/widget.html
  - custard-calendar/docs/scoop.html
  - custard-calendar/docs/group.html
  - custard-calendar/docs/privacy.html
  - custard-calendar/docs/sw.js
autonomous: false
requirements:
  - STOR-01
  - STOR-02
  - STOR-03
  - STOR-04
  - STOR-05
must_haves:
  truths:
    - "Every page with a nav bar renders it via shared-nav.js (no inline nav HTML remains)"
    - "Store indicator is visible on every page when a store is saved in localStorage"
    - "Store selection made on index.html is visible when navigating to calendar.html (cross-page persistence)"
    - "Service worker caches shared-nav.js and delivers fresh content after CACHE_VERSION bump"
    - "All existing Playwright browser tests still pass (no regressions)"
  artifacts:
    - path: "custard-calendar/docs/sw.js"
      provides: "Updated STATIC_ASSETS with shared-nav.js, bumped CACHE_VERSION"
      contains: "shared-nav.js"
    - path: "custard-calendar/docs/index.html"
      provides: "Inline nav replaced with #shared-nav div + shared-nav.js script"
      contains: "shared-nav"
    - path: "custard-calendar/docs/calendar.html"
      provides: "Inline nav replaced with #shared-nav div + shared-nav.js script"
      contains: "shared-nav"
  key_links:
    - from: "custard-calendar/docs/*.html"
      to: "custard-calendar/docs/shared-nav.js"
      via: "script tag"
      pattern: "<script src=\"shared-nav\\.js\"></script>"
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/shared-nav.js"
      via: "STATIC_ASSETS array entry"
      pattern: "shared-nav\\.js"
---

<objective>
Deploy shared-nav.js across all 12 HTML pages by replacing inline nav HTML with the shared-nav placeholder, update the service worker for cache correctness, and verify the full integration works end-to-end.

Purpose: Plan 01 built and tested shared-nav.js in isolation. This plan activates it across the entire site, ensuring every page gets the shared nav bar and store indicator, and the service worker correctly caches the new file.

Output: All 12 HTML files updated, sw.js updated with new cache version, all browser tests green.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

shared-nav.js auto-initializes on DOMContentLoaded, targeting:
```html
<div id="shared-nav"></div>
```

Script load order requirement:
```html
<script src="planner-shared.js"></script>
<script src="shared-nav.js"></script>
```
shared-nav.js depends on `window.CustardPlanner` being available (loaded by planner-shared.js).

For pages that do NOT currently load planner-shared.js (privacy.html), both scripts must be added.

Current inline nav pattern to REMOVE from each page:
```html
<nav class="nav-links" style="margin-top:0.75rem;">
  <a href="index.html">Forecast</a>
  <a href="calendar.html">Calendar</a>
  <!-- ... 9 more links ... -->
</nav>
```

Replace with:
```html
<div id="shared-nav"></div>
```

index.html is SPECIAL -- it has a location-bar with search input, geo button, dropdown, and
current-store badge that other pages don't have. The shared-nav.js store indicator handles store
display for ALL pages. index.html's location-bar/search UI should remain for its hero/empty-state
flow but the `<nav>` block inside `<header>` is still replaced by `<div id="shared-nav">`.

From sw.js:
```javascript
const CACHE_VERSION = 'custard-v7';  // Must become 'custard-v8'
const STATIC_ASSETS = [ ... ];       // Must add './shared-nav.js'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline nav HTML in all 12 pages and add shared-nav.js script tags</name>
  <files>
    custard-calendar/docs/index.html,
    custard-calendar/docs/calendar.html,
    custard-calendar/docs/map.html,
    custard-calendar/docs/radar.html,
    custard-calendar/docs/alerts.html,
    custard-calendar/docs/siri.html,
    custard-calendar/docs/forecast-map.html,
    custard-calendar/docs/quiz.html,
    custard-calendar/docs/widget.html,
    custard-calendar/docs/scoop.html,
    custard-calendar/docs/group.html,
    custard-calendar/docs/privacy.html
  </files>
  <action>
For each of the 12 HTML files with `<nav class="nav-links">` blocks, perform the following changes:

**For all 12 pages:**
1. Find the `<nav class="nav-links"...>...</nav>` block (may have `style="margin-top:0.75rem;"` or not).
2. Replace the entire `<nav>...</nav>` block with `<div id="shared-nav"></div>`.
3. Verify `<script src="planner-shared.js"></script>` exists before the closing `</body>` tag. If missing (privacy.html), add it.
4. Add `<script src="shared-nav.js"></script>` AFTER the `planner-shared.js` script tag. Place it right after planner-shared.js so it loads synchronously and renders nav quickly (per RESEARCH.md Pitfall 3 -- avoid FOUC).

**Pages that need the script tag added (11 already have planner-shared.js; 1 does not):**
- privacy.html: Add BOTH `<script src="planner-shared.js"></script>` AND `<script src="shared-nav.js"></script>`.
- All other 11 pages: Add only `<script src="shared-nav.js"></script>` after the existing planner-shared.js tag.

**Special handling for index.html:**
- index.html has a `location-bar` div (lines 28-45) with search input, geo button, dropdown, and current-store badge. This is the hero/first-visit store discovery UI specific to index.html.
- KEEP the location-bar div and its contents. It is separate from the nav.
- ONLY replace the `<nav class="nav-links"...>...</nav>` block (lines 47-59) with `<div id="shared-nav"></div>`.
- The shared-nav.js store indicator in the header will coexist with index.html's location-bar. index.html may need a small CSS tweak or a JS check to avoid duplicate store displays (if shared-nav's indicator and the location-bar's current-store badge both show). Approach: add `data-page="index"` attribute to the `<div id="shared-nav">` on index.html so shared-nav.js can detect it and optionally hide the store indicator when the location-bar is active. OR simply let both coexist for now (the store indicator is compact and supplementary). Use your judgment per Claude's discretion.

**Pages to modify (12 total):**
1. `index.html` -- nav at line 47 (special: keep location-bar)
2. `calendar.html` -- nav at line 27
3. `map.html` -- nav at line 27
4. `radar.html` -- nav at line 25
5. `alerts.html` -- nav at line 25
6. `siri.html` -- nav at line 139
7. `forecast-map.html` -- nav at line 27
8. `quiz.html` -- nav at line 445
9. `widget.html` -- nav at line 335
10. `scoop.html` -- nav at line 25
11. `group.html` -- nav at line 236
12. `privacy.html` -- nav at line 22

After modification, do a quick scan to confirm NO remaining `<nav class="nav-links"` exists in any docs/*.html file. If any remain, fix them.
  </action>
  <verify>
    <automated>grep -r 'nav class="nav-links"' /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/docs/*.html; echo "EXIT:$?"</automated>
  </verify>
  <done>All 12 HTML files have inline nav replaced with `<div id="shared-nav"></div>`. All 12 files have `<script src="shared-nav.js"></script>` after planner-shared.js. No `<nav class="nav-links">` blocks remain in any docs/*.html file. grep returns 0 matches.</done>
</task>

<task type="auto">
  <name>Task 2: Update sw.js cache and run full test suite</name>
  <files>custard-calendar/docs/sw.js, custard-calendar/worker/test/browser/nav-clickthrough.spec.mjs</files>
  <action>
1. Open `docs/sw.js` and make two changes:
   - Change `CACHE_VERSION` from `'custard-v7'` to `'custard-v8'`.
   - Add `'./shared-nav.js'` to the `STATIC_ASSETS` array (place it after `'./planner-shared.js'` for logical grouping).

2. Run the full Playwright browser test suite to verify:
   - `cd custard-calendar/worker && npm run test:browser -- --workers=1`
   - All existing tests (nav-clickthrough, etc.) must pass. The nav-clickthrough test will need to adapt because it currently looks for `header nav.nav-links` -- after our changes, the nav is rendered by JS into `#shared-nav`. The test locator may need to change to `#shared-nav nav.nav-links` or similar.
   - If nav-clickthrough.spec.mjs fails because the nav locator changed, update it to find nav within `#shared-nav` instead of `header nav.nav-links`. The nav still has class `nav-links` (rendered by shared-nav.js), it is just inside `#shared-nav` now.

3. Run the shared-nav-store tests:
   - `cd custard-calendar/worker && npx playwright test test/browser/shared-nav-store.spec.mjs --reporter=list`
   - All STOR-01 through STOR-05 tests must pass.

4. Run the existing Worker unit tests to verify no regressions:
   - `cd custard-calendar/worker && npm test`
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npm run test:browser -- --workers=1 2>&1 | tail -10</automated>
  </verify>
  <done>sw.js has CACHE_VERSION = 'custard-v8' and STATIC_ASSETS includes './shared-nav.js'. All Playwright browser tests pass (including nav-clickthrough with updated locators). All Worker unit tests pass. STOR-01 through STOR-05 tests pass.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Visual verification of shared nav and store indicator across all pages</name>
  <files>custard-calendar/docs/index.html</files>
  <action>
Human verifies the deployed shared navigation bar and store indicator across all 12 pages.

What was built: Shared navigation bar and store indicator deployed across all 12 pages. Every page now renders its nav via shared-nav.js instead of inline HTML. Store indicator shows saved store, first-visit geolocation flow works, and store picker opens on "change" tap.

Steps to verify:
1. Start local server if not running: `cd custard-calendar/docs && python3 -m http.server 4173`
2. Open http://127.0.0.1:4173/index.html in a browser.
3. Clear localStorage (DevTools > Application > Local Storage > clear all).
4. First visit (STOR-01, STOR-02): Reload the page. You should see a prompt like "Showing flavors for [Store Name] -- change?" in the header area (IP geolocation may pick a nearby store or fallback to picker).
5. Confirm store: Click the confirm/accept button. The prompt should transition to a compact store indicator showing the store name.
6. Cross-page persistence (STOR-05): Navigate to calendar.html. The same store name should appear in the header store indicator.
7. Change store (STOR-04): Tap "change" on the store indicator. A store picker should appear with a search input. Type a store name (e.g., "Mt. Horeb"). Matching stores should filter in the list. Select one. Picker closes, indicator updates.
8. Nav rendering: All pages should show the same 11 nav links. The current page link should be highlighted (nav-active class).
9. Mobile (375px): In DevTools, switch to iPhone SE or 375px viewport. Store indicator should not overflow. Nav links should wrap/scroll appropriately.

Resume: Type "approved" if everything works, or describe any issues you see.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human visual verification"</automated>
  </verify>
  <done>User confirms all visual checks pass: nav renders on every page, store indicator works, first-visit prompt appears, store picker opens and filters correctly, cross-page persistence works, 375px layout is not broken.</done>
</task>

</tasks>

<verification>
1. No inline `<nav class="nav-links">` remains in any docs/*.html file
2. All 12 pages have `<div id="shared-nav">` and `<script src="shared-nav.js">`
3. sw.js CACHE_VERSION bumped to v8, STATIC_ASSETS includes shared-nav.js
4. All Playwright browser tests pass (both new STOR tests and existing tests)
5. All Worker unit tests pass (npm test)
6. Human verified the visual result on localhost
</verification>

<success_criteria>
- Every page renders its nav via shared-nav.js (zero inline nav blocks remain)
- Store indicator visible on every page when a store is saved
- First-visit geolocation and confirmation prompt work on any entry page
- Store picker works with type-ahead search
- Service worker correctly caches shared-nav.js
- All automated tests pass, human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
