---
phase: 03-compare-page
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - custard-calendar/docs/sw.js
autonomous: false
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, COMP-05, COMP-06, COMP-07, COMP-08]

must_haves:
  truths:
    - "Service worker pre-caches compare.html and compare-page.js"
    - "CACHE_VERSION is bumped so returning users get fresh content"
    - "All Playwright compare tests pass"
    - "All existing browser tests pass with no regressions"
    - "All worker unit tests pass"
    - "User visually confirms compare page layout at 375px"
  artifacts:
    - path: "custard-calendar/docs/sw.js"
      provides: "Updated STATIC_ASSETS and CACHE_VERSION"
      contains: "compare-page.js"
  key_links:
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/compare.html"
      via: "STATIC_ASSETS entry ./compare.html"
      pattern: "compare.html"
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/compare-page.js"
      via: "STATIC_ASSETS entry ./compare-page.js"
      pattern: "compare-page.js"
---

<objective>
Update the service worker to cache Compare page assets, run full regression test suites, and get visual verification that the Compare page works at 375px.

Purpose: This is the deployment gate. The service worker must cache new files and bump CACHE_VERSION so returning users get the Compare page. Test suites confirm no regressions. Visual checkpoint confirms the page looks correct.

Output: Updated sw.js, all tests green, user-approved visual state
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-compare-page/03-CONTEXT.md
@.planning/phases/03-compare-page/03-01-SUMMARY.md
@.planning/phases/03-compare-page/03-02-SUMMARY.md

<interfaces>
From custard-calendar/docs/sw.js (current state):
- CACHE_VERSION = 'custard-v12' (set in Phase 2 Plan 03)
- STATIC_ASSETS array lists all pre-cached files
- Install event pre-caches STATIC_ASSETS
- Activate event deletes old cache versions
- Must add ./compare.html and ./compare-page.js
- Must bump to 'custard-v13'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update service worker and run full test suites</name>
  <files>custard-calendar/docs/sw.js</files>
  <action>
    1. Open custard-calendar/docs/sw.js
    2. Bump CACHE_VERSION from 'custard-v12' to 'custard-v13'
    3. Add './compare.html' and './compare-page.js' to STATIC_ASSETS array (insert after ./today-page.js to keep logical grouping)
    4. No other changes to sw.js logic -- install, activate, fetch handlers remain the same

    Then run all test suites in sequence:
    - Worker unit tests: cd custard-calendar/worker && npm test
    - Compare Playwright tests: cd custard-calendar/worker && npx playwright test test/browser/compare-*.spec.mjs --workers=1
    - Full browser test suite: cd custard-calendar/worker && npm run test:browser -- --workers=1

    All must pass with zero failures. If any test fails, diagnose and fix before proceeding. Acceptable: existing skipped tests from Phase 2 (5 tests for removed features).
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npm test && npx playwright test test/browser/compare-*.spec.mjs --workers=1 && npm run test:browser -- --workers=1 2>&1 | tail -30</automated>
  </verify>
  <done>sw.js has CACHE_VERSION 'custard-v13', STATIC_ASSETS includes compare.html and compare-page.js. All worker unit tests pass. All compare Playwright tests pass. Full browser suite passes with no new failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Compare page</name>
  <files>custard-calendar/docs/compare.html</files>
  <action>
    Present the completed Compare page for user verification. The page has been built across Plans 01-03 with day-first card stack, accordion expand, exclusion filters, rarity badges, rarity nudge banner, and Google Maps directions links. Service worker updated to cache new assets.

    Note: CORS will block live API calls on localhost (same as Phase 2 verification). The Playwright tests mock API routes and verify all page behaviors. User can verify test evidence OR check the page structure visually.

    Option A -- Test evidence (recommended):
    1. Review Playwright test output: all compare-grid, compare-expand, and compare-filter tests pass
    2. Confirm the test count covers all 8 COMP requirements

    Option B -- Visual inspection at 375px:
    1. Run: cd custard-calendar && python3 -m http.server 4173 --directory docs
    2. Open Chrome DevTools, set viewport to 375px width
    3. Navigate to http://localhost:4173/compare.html
    4. Note: without mocked API data, the page will show loading skeleton or empty state. The SharedNav and page shell should render correctly.
    5. Check: page title, SharedNav in header, no horizontal overflow at 375px

    Verify these behaviors are confirmed by passing tests:
    - 3 day-cards visible (Today, Tomorrow, Day+2)
    - Each day-card has store rows with cone, flavor name, rarity badge
    - Tapping a row expands detail with description + directions link
    - Only one row expanded at a time
    - 6 filter chips visible above grid
    - Toggling filter dims matching rows
    - Single-store user sees prompt
    - 375px layout has no horizontal scroll
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>User approves the Compare page. Phase 3 complete: all COMP-01 through COMP-08 requirements satisfied.</done>
</task>

</tasks>

<verification>
Service worker updated with compare assets and bumped CACHE_VERSION.
All worker unit tests pass (574+ tests).
All compare Playwright tests pass (COMP-01 through COMP-08 covered).
Full browser test suite passes with no regressions.
User approves visual state.
Phase 3 is complete.
</verification>

<success_criteria>
- sw.js CACHE_VERSION is 'custard-v13'
- sw.js STATIC_ASSETS includes ./compare.html and ./compare-page.js
- All test suites pass: worker unit, compare Playwright, full browser
- User approves the Compare page visual verification
- Phase 3 complete: all COMP-01 through COMP-08 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/03-compare-page/03-03-SUMMARY.md`
</output>
