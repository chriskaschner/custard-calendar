---
phase: 02-today-page
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - custard-calendar/docs/style.css
  - custard-calendar/docs/today-page.js
  - custard-calendar/docs/index.html
autonomous: true
requirements: [TDAY-03, TDAY-04]

must_haves:
  truths:
    - "Week-ahead section is a collapsed details element, not visible by default"
    - "Week-ahead expands when user clicks the summary to reveal day cards"
    - "User with 2+ saved stores sees a compact multi-store row with today's flavor at each store"
    - "Tapping a multi-store cell switches the hero card to that store's flavor"
    - "Multi-store row is hidden when user has only 1 saved store"
  artifacts:
    - path: "custard-calendar/docs/style.css"
      provides: "CSS rules for multi-store row (horizontal scroll, cells, mini cones) and updates-cta card"
      contains: ".multi-store-row"
    - path: "custard-calendar/docs/today-page.js"
      provides: "Refined multi-store rendering and details week-ahead logic"
    - path: "custard-calendar/docs/index.html"
      provides: "Any HTML adjustments needed for CSS integration"
  key_links:
    - from: "custard-calendar/docs/today-page.js"
      to: "CustardPlanner.getDrivePreferences"
      via: "function call for saved store slugs"
      pattern: "getDrivePreferences"
    - from: ".multi-store-cell click"
      to: "sharednav:storechange"
      via: "CustomEvent dispatch on cell tap"
      pattern: "sharednav:storechange"
    - from: "details#week-section"
      to: "today-page.js renderWeekStrip"
      via: "JS populates .week-strip inside collapsed details"
      pattern: "week-strip"
---

<objective>
Add CSS for the multi-store glance row, updates CTA card, and polish the details week-ahead and hero card styles. Ensure the multi-store row works end-to-end: horizontal scroll at 375px, tapping switches hero, hidden for single-store users. Ensure the details week-ahead collapses/expands correctly.

Purpose: Plan 01 creates the JS logic and HTML structure. This plan adds the CSS rules that make the multi-store row visually correct at 375px, styles the CTA card, and validates the full interactive flow.

Output: Updated style.css with new rules, any JS/HTML tweaks needed, all TDAY-03 and TDAY-04 tests pass
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-today-page/02-CONTEXT.md
@.planning/phases/02-today-page/02-RESEARCH.md
@.planning/phases/02-today-page/02-01-SUMMARY.md

<interfaces>
<!-- Key contracts from Plan 01 output -->

From docs/today-page.js (created in Plan 01):
  window.CustardToday.init()            -- initializes page on DOMContentLoaded
  window.CustardToday.selectStore(slug) -- switches hero card to given store

HTML structure from Plan 01:
  #multi-store-section (hidden by default)
    .multi-store-row (container for cells)
      button.multi-store-cell[data-slug] (JS-rendered per store)
        .multi-store-cone (mini SVG cone)
        .multi-store-flavor (flavor name text)
        .multi-store-name (store city/name text)

  details#week-section (collapsed by default)
    summary > h2 "Week Ahead"
    .week-strip (flex horizontal scroll of day cards)

  #updates-cta (hidden until data loads)
    .updates-cta-card
      strong "Want this every day?"
      p description
      a.btn-primary "Set it up"

Existing CSS patterns in style.css:
  .week-strip { display: flex; gap: 0.5rem; overflow-x: auto; }
  .day-card { min-width: 120px; ... }
  details summary { cursor: pointer; color: #005696; }
  .btn-primary { background: #005696; color: white; padding: 0.5rem 1rem; border-radius: 0.5rem; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS for multi-store row, CTA card, and hero card polish</name>
  <files>custard-calendar/docs/style.css</files>
  <action>
Add new CSS rules to style.css for the elements created in Plan 01. Do NOT remove existing rules that other pages may use (e.g., calendar-preview CSS is used only by index.html so it CAN be removed, but drive-related CSS may be used by scoop.html so leave it).

**Multi-store row styles:**
```css
.multi-store-row {
  display: flex;
  gap: 0.75rem;
  overflow-x: auto;
  padding: 0.5rem 0;
  -webkit-overflow-scrolling: touch;
  scroll-snap-type: x mandatory;
}

.multi-store-cell {
  flex: 0 0 auto;
  min-width: 100px;
  max-width: 140px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.25rem;
  padding: 0.5rem;
  border: 1px solid #e0e0e0;
  border-radius: 0.5rem;
  background: white;
  cursor: pointer;
  scroll-snap-align: start;
  text-align: center;
  font: inherit;
  color: inherit;
}

.multi-store-cell:hover,
.multi-store-cell:focus {
  border-color: #005696;
  box-shadow: 0 0 0 1px #005696;
  outline: none;
}

.multi-store-cell.active {
  border-color: #005696;
  background: #f0f6fc;
}

.multi-store-cone {
  width: 40px;
  height: 48px;
}

.multi-store-cone svg {
  width: 100%;
  height: 100%;
}

.multi-store-flavor {
  font-size: 0.8125rem;
  font-weight: 600;
  line-height: 1.2;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
}

.multi-store-name {
  font-size: 0.6875rem;
  color: #666;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 100%;
}
```

**Updates CTA card styles:**
```css
.updates-cta-card {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
  padding: 1rem;
  border: 1px solid #e0e0e0;
  border-radius: 0.75rem;
  background: #f8f9fa;
  margin: 1rem 0;
}

.updates-cta-card strong {
  font-size: 1rem;
  color: #005696;
}

.updates-cta-card p {
  font-size: 0.875rem;
  color: #555;
  margin: 0;
}

.updates-cta-card .btn-primary {
  align-self: flex-start;
  text-decoration: none;
  display: inline-block;
}
```

**Hero card polish -- remove card header to simplify:**
Remove or override `.today-card-header` and `.today-card-label` rules since the header div is removed in Plan 01. The hero card should show just the flavor body (cone + text). Ensure `.today-flavor-body` flex layout has cone at ~120px and text fills remaining space:

```css
.today-flavor-body {
  display: flex;
  gap: 1rem;
  align-items: flex-start;
}

.today-flavor-cone.cone-lg {
  flex: 0 0 auto;
  width: 120px;
}

.today-flavor-cone.cone-lg svg {
  width: 100%;
  height: auto;
}
```

**Calendar preview CSS cleanup:**
Remove CSS rules that are ONLY used by `#calendar-preview-section` (which is deleted in Plan 01). These classes are safe to remove:
- `.calendar-preview-section`, `.calendar-preview-head`, `.calendar-preview-grid`, `.calendar-preview-surface`, `.calendar-preview-app`, `.calendar-preview-event`, `.calendar-preview-link`
- `.cal-event`, `.cal-event-color`, `.cal-event-body`, `.cal-event-title`, `.cal-event-date`, `.cal-event-location`, `.cal-event-desc`
- `.apple-cal-card`, `.apple-cal-row`, `.apple-cal-dot`, `.apple-cal-main`, `.apple-cal-title`, `.apple-cal-meta`, `.apple-cal-location`

Do NOT remove `.today-predictions` or `.today-badge` CSS yet -- other code may reference them. Leave them for now.

**Section spacing:**
Add margin-top to each section in the below-fold flow to ensure consistent spacing:
```css
#signals-section { margin-top: 0.75rem; }
#multi-store-section { margin-top: 1rem; }
#updates-cta { margin-top: 1rem; }
details#week-section { margin-top: 1rem; }
```
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx playwright test test/browser/today-hero.spec.mjs test/browser/today-multistore.spec.mjs test/browser/today-week-ahead.spec.mjs --workers=1 2>&1 | tail -20</automated>
  </verify>
  <done>
- style.css contains .multi-store-row, .multi-store-cell, .multi-store-cone, .multi-store-flavor, .multi-store-name rules
- style.css contains .updates-cta-card rules
- .today-flavor-cone.cone-lg is sized at 120px width
- Calendar preview CSS rules removed (only index.html used them)
- All TDAY Playwright tests pass including TDAY-03 (collapsed details) and TDAY-04 (multi-store row)
- Existing browser tests still pass (no CSS regression)
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix any integration issues and verify multi-store tap-to-switch</name>
  <files>
    custard-calendar/docs/today-page.js,
    custard-calendar/docs/index.html
  </files>
  <action>
After CSS is in place, verify the full interactive flow works. This task handles any JS/HTML tweaks needed after Plan 01's output is CSS-styled:

1. **Verify multi-store cell tap dispatches event and switches hero:** Open index.html, set localStorage to have 2 stores, confirm tapping a multi-store cell updates the hero card (cone + flavor name change) and highlights the tapped cell with `.active` class while removing `.active` from the previous cell.

2. **Verify details#week-section behavior:** Confirm the `<details>` element collapses the week strip by default. Clicking the summary opens it. The existing `.week-strip` flex scroll layout works inside `<details>`.

3. **Verify rarity badge placement in hero:** When the API returns rarity data, the `#today-rarity` div should show the rarity badge inline below the description. When no rarity data, `#today-rarity` stays hidden.

4. **Multi-store row active state:** In today-page.js, when rendering the multi-store row, add `.active` class to the cell matching the current primary store slug. When a cell is tapped, move `.active` to the tapped cell.

5. **Edge case: multi-store fetch failure:** If a fetch for a single store's today data fails, render that cell with a "No data" placeholder instead of hiding the entire row. Only hide the row if ALL fetches fail or < 2 stores exist.

6. **Update Playwright tests if needed:** If any test assertions need adjusting based on actual rendered output, update them. The tests should be GREEN after this task.

Run the full browser test suite to ensure no regressions: `cd custard-calendar/worker && npm run test:browser -- --workers=1`
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npm run test:browser -- --workers=1 2>&1 | tail -20</automated>
  </verify>
  <done>
- Multi-store cell tap switches hero card to that store's flavor
- Active cell gets .active class highlighting
- Details week-ahead collapses/expands correctly
- Rarity badge shows/hides correctly based on data
- Failed multi-store fetch shows "No data" in cell, does not crash row
- All TDAY Playwright tests pass
- Full browser test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
1. All 3 TDAY Playwright test files pass: `cd custard-calendar/worker && npx playwright test test/browser/today-hero.spec.mjs test/browser/today-multistore.spec.mjs test/browser/today-week-ahead.spec.mjs --workers=1`
2. Full browser test suite passes: `cd custard-calendar/worker && npm run test:browser -- --workers=1`
3. Multi-store row appears with 2+ stores and is hidden with 1 store
4. Details week-ahead is collapsed by default and expandable
5. No CSS regression on other pages (scoop.html, map.html, etc.)
</verification>

<success_criteria>
- Week-ahead is a collapsed `<details>` element (TDAY-03)
- Multi-store row shows today's flavor at each saved store when 2+ exist (TDAY-04)
- Tapping a multi-store cell switches the hero card
- All automated tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-today-page/02-02-SUMMARY.md`
</output>
