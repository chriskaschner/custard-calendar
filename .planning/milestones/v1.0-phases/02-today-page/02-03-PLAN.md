---
phase: 02-today-page
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - custard-calendar/docs/sw.js
autonomous: false
requirements: [TDAY-01, TDAY-02, TDAY-03, TDAY-04, TDAY-05, TDAY-06, TDAY-07]

must_haves:
  truths:
    - "Service worker caches today-page.js and no longer pre-caches todays-drive.js"
    - "CACHE_VERSION is bumped to force fresh content delivery"
    - "Full browser test suite passes"
    - "Full worker unit test suite passes"
    - "The simplified today page looks correct at 375px viewport"
  artifacts:
    - path: "custard-calendar/docs/sw.js"
      provides: "Updated service worker with today-page.js in STATIC_ASSETS and bumped CACHE_VERSION"
      contains: "today-page.js"
  key_links:
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/today-page.js"
      via: "STATIC_ASSETS array entry for pre-caching"
      pattern: "today-page.js"
---

<objective>
Update the service worker to cache today-page.js, keep todays-drive.js (scoop.html needs it), bump CACHE_VERSION, run the full test suites (browser + worker unit), and perform a visual verification checkpoint at 375px.

Purpose: This final plan ensures the restructured page is correctly cached by the service worker and visually verified by the user. Without the sw.js update, returning users would see stale content.

Output: Updated sw.js, all tests green, user-verified visual layout
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-today-page/02-CONTEXT.md
@.planning/phases/02-today-page/02-01-SUMMARY.md
@.planning/phases/02-today-page/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update service worker and run full test suites</name>
  <files>custard-calendar/docs/sw.js</files>
  <action>
Update sw.js with two changes:

1. **Add today-page.js to STATIC_ASSETS:** Add `'./today-page.js'` to the STATIC_ASSETS array.

2. **Keep todays-drive.js in STATIC_ASSETS:** scoop.html still loads it via script tag. Do not remove it.

3. **Bump CACHE_VERSION:** Change from `'custard-v11'` to `'custard-v12'` to force cache invalidation after the page restructure.

After updating sw.js, run both test suites:
- `cd custard-calendar/worker && npm test` (574+ worker unit tests)
- `cd custard-calendar/worker && npm run test:browser -- --workers=1` (all browser tests)

Both must be fully green. If any test fails, investigate and fix the root cause.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npm test 2>&1 | tail -5 && npm run test:browser -- --workers=1 2>&1 | tail -10</automated>
  </verify>
  <done>
- sw.js CACHE_VERSION is 'custard-v12'
- sw.js STATIC_ASSETS includes './today-page.js'
- sw.js STATIC_ASSETS still includes './todays-drive.js' (scoop.html needs it)
- All worker unit tests pass (574+)
- All browser tests pass (46+)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of simplified Today page at 375px</name>
  <files>custard-calendar/docs/index.html</files>
  <action>
Present the user with verification instructions for the completed Today page restructure. All automated work is complete -- this checkpoint verifies the visual result.

What was built: Radically simplified Today page with focused hero card (cone + flavor name + description + rarity badge), collapsed week-ahead via details element, multi-store glance row (if 2+ stores saved), flavor signal nudge, and "Want this every day?" CTA. All clutter removed (Drive cards, calendar preview, predictions, score badges). Page logic extracted from ~875 lines of inline JS into today-page.js IIFE module.
  </action>
  <verify>
Steps for user to verify:
1. Start local server: `cd custard-calendar/docs && python3 -m http.server 4173 --bind 127.0.0.1`
2. Open Chrome DevTools, set viewport to 375x667 (iPhone SE)
3. Visit http://127.0.0.1:4173/index.html
4. If no store is saved, see the "New here?" empty state with "Find your store" button
5. Select a store (e.g., Mt. Horeb) via the SharedNav store picker
6. Verify hero card appears above the fold: cone image on left (~120px), flavor name and description on right
7. If the flavor has rarity data, verify a rarity badge appears below the description
8. Check that NO Drive ranking cards, calendar preview, or prediction bars appear
9. Scroll down: verify "Week Ahead" is a collapsed section (click to expand)
10. Scroll down: verify "Want this every day?" CTA card with link to calendar.html
11. If 2+ stores saved, verify the multi-store glance row shows between signal and week-ahead
12. Visit http://127.0.0.1:4173/scoop.html to confirm it still works (todays-drive.js intact)
  </verify>
  <done>User types "approved" confirming the Today page looks correct at 375px, or describes issues for a follow-up fix.</done>
</task>

</tasks>

<verification>
1. sw.js updated: `grep -c "today-page.js" custard-calendar/docs/sw.js` returns 1
2. CACHE_VERSION bumped: `grep "CACHE_VERSION" custard-calendar/docs/sw.js` shows v12
3. Worker unit tests: `cd custard-calendar/worker && npm test` all pass
4. Browser tests: `cd custard-calendar/worker && npm run test:browser -- --workers=1` all pass
5. Visual check at 375px confirms hero above fold, no clutter, progressive disclosure works
</verification>

<success_criteria>
- Service worker correctly caches the new page assets
- All automated test suites green (worker + browser)
- User visually confirms the simplified Today page meets expectations at 375px
- No regression on other pages (scoop.html still has Drive features)
</success_criteria>

<output>
After completion, create `.planning/phases/02-today-page/02-03-SUMMARY.md`
</output>
