---
phase: 02-today-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custard-calendar/worker/test/browser/today-hero.spec.mjs
  - custard-calendar/worker/test/browser/today-multistore.spec.mjs
  - custard-calendar/worker/test/browser/today-week-ahead.spec.mjs
  - custard-calendar/docs/today-page.js
  - custard-calendar/docs/index.html
autonomous: true
requirements: [TDAY-01, TDAY-02, TDAY-05, TDAY-06, TDAY-07]

must_haves:
  truths:
    - "User sees today's flavor (cone image, flavor name, description) above the fold at 375px"
    - "Rarity badge displays on the hero card when the flavor is rare"
    - "One contextual flavor signal displays inline when relevant"
    - "'Want this every day?' CTA is present and links to calendar.html"
    - "Drive ranking cards, calendar preview, hero duplication, predictions, and score badges are absent"
  artifacts:
    - path: "custard-calendar/docs/today-page.js"
      provides: "CustardToday IIFE module with all page logic extracted from inline script"
      contains: "window.CustardToday"
    - path: "custard-calendar/docs/index.html"
      provides: "Simplified HTML shell without inline JS block or removed sections"
    - path: "custard-calendar/worker/test/browser/today-hero.spec.mjs"
      provides: "Playwright tests for TDAY-01, TDAY-02, TDAY-05, TDAY-06, TDAY-07"
    - path: "custard-calendar/worker/test/browser/today-multistore.spec.mjs"
      provides: "Playwright test scaffold for TDAY-04"
    - path: "custard-calendar/worker/test/browser/today-week-ahead.spec.mjs"
      provides: "Playwright test scaffold for TDAY-03"
  key_links:
    - from: "custard-calendar/docs/index.html"
      to: "custard-calendar/docs/today-page.js"
      via: "script tag loading CustardToday IIFE"
      pattern: "script src=\"today-page.js\""
    - from: "custard-calendar/docs/today-page.js"
      to: "/api/v1/today"
      via: "fetch for hero card data including rarity"
      pattern: "api/v1/today"
    - from: "custard-calendar/docs/today-page.js"
      to: "sharednav:storechange"
      via: "CustomEvent listener for store changes"
      pattern: "sharednav:storechange"
---

<objective>
Extract ~875 lines of inline JS from index.html into a today-page.js IIFE module, simplify the HTML to remove clutter sections (Drive, calendar preview, predictions, score badges), restructure the hero card layout (cone left ~120px, text right, rarity badge), add flavor signal nudge and "Want this every day?" CTA, and create Playwright test scaffolds for all TDAY requirements.

Purpose: This is the core restructuring plan that transforms the cluttered index.html into a focused "what's the flavor?" answer. The JS extraction makes the page maintainable and testable. The HTML simplification removes five sections that distract from the single-flavor hero.

Output: today-page.js IIFE, simplified index.html, 3 Playwright test files
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-today-page/02-CONTEXT.md
@.planning/phases/02-today-page/02-RESEARCH.md
@.planning/phases/02-today-page/02-VALIDATION.md

<interfaces>
<!-- Existing globals and functions the executor needs. Extracted from codebase. -->

From docs/planner-shared.js (loaded before today-page.js):
  window.CustardPlanner.WORKER_BASE        -- Worker API base URL string
  window.CustardPlanner.BRAND_COLORS       -- { brand: hexColor } map
  window.CustardPlanner.BRAND_DISPLAY      -- { brand: displayName } map
  window.CustardPlanner.brandFromSlug(slug) -- returns brand string
  window.CustardPlanner.escapeHtml(str)    -- HTML entity escaping
  window.CustardPlanner.getPrimaryStoreSlug() -- returns current store slug from localStorage
  window.CustardPlanner.setPrimaryStoreSlug(slug) -- saves store slug to localStorage
  window.CustardPlanner.buildTimeline(forecast, confirmed, predicted) -- returns day[] array
  window.CustardPlanner.getDrivePreferences({ stores }) -- returns { activeRoute: { stores: string[] } }
  window.CustardPlanner.fetchSignals(workerBase, slug, sectionEl, listEl, limit) -- fetches and renders signals
  window.CustardPlanner.initShareButton(mountId) -- share button setup

From docs/cone-renderer.js (loaded before today-page.js):
  window.renderMiniConeSVG(flavorName)           -- returns SVG string for small cone
  window.renderMiniConeHDSVG(flavorName, scale)  -- returns SVG string for large cone (default scale=5 -> 90x105px)

From docs/shared-nav.js:
  CustomEvent 'sharednav:storechange' on document -- detail: { slug: string }
  window.SharedNav.showStorePicker()              -- opens store picker overlay

API response shape for /api/v1/today?slug=X:
  { slug, flavor, description, date, rarity: { appearances, avg_gap_days, label } | null }

API response shape for /api/v1/forecast/{slug}:
  { slug, forecast: [{ date, flavor, description, type }], fetchedAt }

API response shape for /api/v1/flavors?slug=X:
  { slug, name, flavors: [{ date, title, description }] }

Existing Playwright test pattern from worker/test/browser/shared-nav-store.spec.mjs:
  - Use context.route() for cross-origin API mocking
  - Navigate to /index.html
  - Manipulate localStorage for store state
  - Assert DOM elements visible/hidden
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright test scaffolds for all TDAY requirements</name>
  <files>
    custard-calendar/worker/test/browser/today-hero.spec.mjs,
    custard-calendar/worker/test/browser/today-multistore.spec.mjs,
    custard-calendar/worker/test/browser/today-week-ahead.spec.mjs
  </files>
  <action>
Create three Playwright test files following the established pattern from shared-nav-store.spec.mjs (context.route() for API mocking, localStorage manipulation for store state).

**today-hero.spec.mjs** -- covers TDAY-01, TDAY-02, TDAY-05, TDAY-06, TDAY-07:
- Create a shared setup function that mocks these API endpoints via context.route():
  - `**/stores.json*` -> mock stores manifest (3 stores: mt-horeb, verona, madison-east)
  - `**/api/v1/today?slug=*` -> mock today response with rarity data: `{ slug: "mt-horeb", flavor: "Chocolate Eclair", description: "Rich chocolate custard with eclair pieces", date: "2026-03-07", rarity: { appearances: 3, avg_gap_days: 120, label: "Rare" } }`
  - `**/api/v1/forecast/*` -> mock forecast response with 7-day forecast array
  - `**/api/v1/flavors?slug=*` -> mock flavors response
  - `**/api/v1/signals/*` -> mock signals response: `{ signals: [{ headline: "Peaks on Sundays", explanation: "This flavor appears 2x more on Sundays" }] }`
  - `**/api/v1/geolocate` -> mock geo response
- Set localStorage `custard-primary` to "mt-horeb" before each test
- Tests (all should FAIL initially since today-page.js does not exist yet):
  1. TDAY-01: "hero card shows cone, flavor name, and description" -- assert `#today-section` visible, `#today-cone` contains SVG, `#today-flavor` text is "Chocolate Eclair", `#today-desc` has text content
  2. TDAY-02: "rarity badge displays when flavor is rare" -- assert `#today-rarity` is visible, contains text matching /Rare/, contains text matching /120 days/
  3. TDAY-05: "flavor signal displays when relevant" -- assert `#signals-section` is visible, contains text matching /Peaks on Sundays/
  4. TDAY-06: "want this every day CTA links to calendar" -- assert element with text "Want this every day?" is visible, find link/button with href containing "calendar"
  5. TDAY-07: "removed sections are absent" -- assert `#todays-drive-section` is hidden or removed, `#calendar-preview-section` is hidden or removed, `#today-predictions` is hidden or removed, `#today-badge` is hidden or empty

**today-multistore.spec.mjs** -- covers TDAY-04:
- Same API mock setup, but set localStorage to have 2+ stores via custard:v1:preferences key with activeRoute.stores = ["mt-horeb", "verona"]
- Mock /api/v1/today for both slugs with different flavors
- Tests:
  1. TDAY-04: "multi-store row shows when 2+ stores saved" -- assert `#multi-store-section` is visible, contains at least 2 cells with flavor names
  2. "multi-store row hidden when only 1 store" -- clear preferences to single store, assert `#multi-store-section` is hidden

**today-week-ahead.spec.mjs** -- covers TDAY-03:
- Same API mock setup with 7-day forecast
- Tests:
  1. TDAY-03: "week ahead is collapsed details element" -- assert `details#week-section` exists, is not open by default, `.week-strip` is not visible
  2. "week ahead expands on click" -- click the summary element, assert `.week-strip` becomes visible, contains day cards

Use `var` in any inline evaluate() calls. Use ES module imports (import { test, expect } from "@playwright/test").
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx playwright test test/browser/today-hero.spec.mjs test/browser/today-multistore.spec.mjs test/browser/today-week-ahead.spec.mjs --workers=1 2>&1 | tail -20</automated>
  </verify>
  <done>Three test files exist. Tests run but FAIL (expected -- RED phase: the page logic does not exist yet). No test file has syntax errors or import failures. The test runner executes all test files without crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Extract inline JS to today-page.js IIFE and simplify index.html</name>
  <files>
    custard-calendar/docs/today-page.js,
    custard-calendar/docs/index.html
  </files>
  <action>
This is the core task. Two files change: today-page.js is created, index.html is restructured.

**Create today-page.js as window.CustardToday IIFE:**

Follow the IIFE Revealing Module pattern. Use `var` throughout (ES5 convention, matching planner-shared.js, shared-nav.js, cone-renderer.js). Structure:

```
var CustardToday = (function() {
  'use strict';
  // Private state
  var _currentSlug = null;
  var _allStores = [];
  var _flavorColors = {};
  // ... DOM refs (getElementById calls)
  // ... utility functions (toISODate, addDays, timeSince, escapeHtml alias)
  // ... data fetching (loadForecast, loadStores, loadFlavorColors)
  // ... rendering (renderHeroCard, renderRarity, renderWeekStrip, renderMultiStoreRow, renderSignalNudge)
  // ... event handlers (onStoreChange, onRetry)
  // ... init()
  return { init: init, selectStore: selectStore };
})();
```

Extract ALL functions from the current inline `<script>` block (lines 217-1091 of index.html) into today-page.js. Key changes during extraction:

1. **Remove all CustardDrive references:** Do not include `driveController`, `CustardDrive.mount()`, or the `onPrimaryStoreChange` callback. todays-drive.js is no longer loaded on this page.

2. **Remove calendar preview rendering:** Do not include `updateCalendarPreview()` or any DOM refs for `sample-google-*` / `sample-apple-*` elements.

3. **Remove prediction rendering:** Do not include prediction bar rendering code. Remove the `today-predictions` population. Remove `today-badge` confirmed/estimated badge logic.

4. **Remove today-meta rendering:** Do not populate `today-store` or `today-freshness` elements (they will be removed from HTML).

5. **Hero card rendering changes:**
   - Use `renderMiniConeHDSVG(day.flavor, 6)` for hero cone (scale 6 -> ~108x126px, closest to ~120px target per research)
   - Render: cone left, text right with flavor name + description + rarity badge
   - Call `renderRarity(todayData && todayData.rarity)` as part of hero rendering
   - No badge, no meta, no predictions in the hero card

6. **Add flavor signal nudge:** After hero card renders, call `CustardPlanner.fetchSignals(WORKER_BASE, slug, signalsSection, signalsList, 1)` to show one signal. The existing function handles everything (fetch, render, error swallowing). Just unhide `#signals-section` container if signal data exists. The fetchSignals function from planner-shared.js already renders into the provided elements.

7. **Add multi-store glance row rendering:** Create `renderMultiStoreRow()` function:
   - Get saved stores: `CustardPlanner.getDrivePreferences({ stores: _allStores }).activeRoute.stores`
   - Only render if storeSlugs.length >= 2
   - For each store slug, fetch `/api/v1/today?slug=X` in parallel via Promise.all
   - Each cell is a `<button>` with class `multi-store-cell`, data-slug attribute
   - Cell content: `renderMiniConeSVG(flavor)` + flavor name + store city/name (from _allStores manifest)
   - Click handler: dispatch `sharednav:storechange` CustomEvent with { slug }, call `selectStore(slug)` to update hero
   - Container: `#multi-store-row` inside `#multi-store-section`, horizontal scroll via CSS
   - Unhide `#multi-store-section` when cells rendered, keep hidden if < 2 stores

8. **Add "Want this every day?" CTA:** After data loads successfully, unhide `#updates-cta`. The HTML will contain the CTA structure (see HTML changes below).

9. **Listen for sharednav:storechange:** `document.addEventListener('sharednav:storechange', function(e) { selectStore(e.detail.slug); });`

10. **init() function:** On DOMContentLoaded, call `Promise.all([loadStores(), loadFlavorColors()])` then check for saved store slug, show empty state or load forecast. This mirrors the current init flow but without Drive mounting.

11. **Error/loading states:** Keep the existing showLoading/showError/hideLoading pattern. On successful load, hide loading and empty state, show today-section.

**Restructure index.html:**

1. Remove the entire inline `<script>` block (lines 217 through the closing `</script>` before the closing `</body>` tag).

2. Remove HTML sections:
   - `#todays-drive-section` (lines 158-160) -- delete entirely
   - `#calendar-preview-section` (lines 162-197) -- delete entirely
   - `<script src="todays-drive.js"></script>` tag (line 216) -- delete
   - `#today-predictions` div (line 115) -- delete
   - `#today-badge` span (line 105) -- delete
   - `.today-meta` div containing `#today-store` and `#today-freshness` (lines 116-119) -- delete
   - `#watch-banner-container` div (line 129) -- delete

3. Restructure hero card HTML. The `#today-section` should contain:
   ```html
   <section id="today-section" hidden>
     <div class="today-card" id="today-card">
       <div class="today-flavor-body">
         <div class="today-flavor-cone cone-lg" id="today-cone" aria-hidden="true"></div>
         <div class="today-flavor-content">
           <div class="today-flavor-name" id="today-flavor"></div>
           <div class="today-flavor-desc" id="today-desc"></div>
           <div class="today-rarity" id="today-rarity" hidden></div>
         </div>
       </div>
     </div>
   </section>
   ```
   (Removed: today-card-header with label and badge, today-predictions, today-meta, today-ctas)

4. Keep `#signals-section` as-is (already exists at lines 124-127).

5. Add multi-store section after signals-section:
   ```html
   <section id="multi-store-section" hidden>
     <div class="multi-store-row" id="multi-store-row"></div>
   </section>
   ```

6. Convert `#week-section` to `<details>`:
   ```html
   <details id="week-section" hidden>
     <summary><h2 style="display:inline;">Week Ahead</h2></summary>
     <div class="week-strip" id="week-strip"></div>
   </details>
   ```
   Remove the `.week-header` div and `#week-freshness` span.

7. Replace `#calendar-cta` section with:
   ```html
   <section id="updates-cta" hidden>
     <div class="updates-cta-card">
       <strong>Want this every day?</strong>
       <p>Get daily flavors in your calendar, on your phone, or via email.</p>
       <a href="calendar.html" class="btn-primary" id="updates-cta-link">Set it up</a>
     </div>
   </section>
   ```

8. Update script tags at bottom of body:
   ```html
   <script src="planner-shared.js"></script>
   <script src="shared-nav.js"></script>
   <script>CustardPlanner.initShareButton('page-share');</script>
   <script src="cone-renderer.js"></script>
   <script src="today-page.js"></script>
   ```
   Note: Remove `<script>var WORKER_BASE = CustardPlanner.WORKER_BASE;</script>` (today-page.js reads it directly). Remove `<script src="todays-drive.js"></script>`. Remove the massive inline `<script>` block.

9. Page structure order after restructuring (within `<main>`):
   empty-state > today-loading > error-state > today-section (hero) > signals-section > multi-store-section > week-section (details) > updates-cta

**Important anti-patterns to avoid:**
- Do NOT use `let`/`const` in today-page.js. Use `var` throughout.
- Do NOT reference CustardDrive anywhere in today-page.js.
- Do NOT directly read localStorage for store preferences. Use CustardPlanner.getDrivePreferences().
- Do NOT build custom accordion JS for week-ahead. Use native `<details>`.
- Do NOT hardcode the Worker API base URL. Use CustardPlanner.WORKER_BASE.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx playwright test test/browser/today-hero.spec.mjs test/browser/today-multistore.spec.mjs test/browser/today-week-ahead.spec.mjs --workers=1 2>&1 | tail -30</automated>
  </verify>
  <done>
- today-page.js exists as a CustardToday IIFE using `var` throughout (~600-700 lines)
- index.html is a simplified HTML shell (~200 lines, no inline JS block)
- index.html does not contain: todays-drive-section, calendar-preview-section, today-predictions, today-badge, today-meta, todays-drive.js script tag, CustardDrive.mount()
- index.html contains: simplified hero card, signals-section, multi-store-section, details#week-section, updates-cta
- Hero card renders cone left + flavor name + description + rarity badge (when applicable)
- Playwright tests from Task 1 pass (GREEN)
- No console errors when loading index.html with a saved store
  </done>
</task>

</tasks>

<verification>
1. All 3 Playwright test files run and pass: `cd custard-calendar/worker && npx playwright test test/browser/today-hero.spec.mjs test/browser/today-multistore.spec.mjs test/browser/today-week-ahead.spec.mjs --workers=1`
2. Existing browser tests still pass: `cd custard-calendar/worker && npm run test:browser -- --workers=1`
3. index.html does not contain inline `<script>` block (only script src tags)
4. index.html does not reference todays-drive.js or CustardDrive
5. today-page.js uses `var` throughout (no `let`/`const`)
6. today-page.js exposes window.CustardToday with init() and selectStore() methods
</verification>

<success_criteria>
- User sees today's flavor (cone, name, description) above fold at 375px
- Rarity badge shows when data includes rarity
- Signal nudge appears when signal data returned
- "Want this every day?" CTA visible and links to calendar.html
- No Drive cards, calendar preview, predictions, or score badges
- Page logic lives in today-page.js, index.html is mostly HTML
- All new Playwright tests pass
- Existing browser test suite does not regress
</success_criteria>

<output>
After completion, create `.planning/phases/02-today-page/02-01-SUMMARY.md`
</output>
