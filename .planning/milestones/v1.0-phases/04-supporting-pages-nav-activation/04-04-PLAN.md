---
phase: 04-supporting-pages-nav-activation
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02, 04-03]
files_modified:
  - custard-calendar/docs/sw.js
autonomous: false
requirements: [FUN-03, NAV-01, NAV-02, NAV-03, NAV-04]

must_haves:
  truths:
    - "Service worker caches fun.html, updates.html, fun-page.js, updates-page.js"
    - "CACHE_VERSION is bumped to force stale cache invalidation"
    - "Full browser test suite passes green"
    - "Visual inspection confirms 4-item nav, footer, fun page, and updates page at 375px"
  artifacts:
    - path: "custard-calendar/docs/sw.js"
      provides: "Updated STATIC_ASSETS with new pages and CACHE_VERSION bump"
      contains: "fun.html.*updates.html.*fun-page.js.*updates-page.js"
  key_links:
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/fun.html"
      via: "STATIC_ASSETS array includes fun.html"
      pattern: "fun\\.html"
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/updates.html"
      via: "STATIC_ASSETS array includes updates.html"
      pattern: "updates\\.html"
---

<objective>
Update service worker to cache all new Phase 4 assets, bump CACHE_VERSION, run full test suite, and visually verify all pages at 375px.

Purpose: Ensures returning users receive the new pages and updated nav. This is the deployment gate -- nothing ships until the full suite is green and visual verification passes.
Output: Updated sw.js, all tests passing, visual verification.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-supporting-pages-nav-activation/04-CONTEXT.md
@.planning/phases/04-supporting-pages-nav-activation/04-01-SUMMARY.md
@.planning/phases/04-supporting-pages-nav-activation/04-02-SUMMARY.md
@.planning/phases/04-supporting-pages-nav-activation/04-03-SUMMARY.md

<interfaces>
From custard-calendar/docs/sw.js (current state):
```javascript
const CACHE_VERSION = 'custard-v13';
const STATIC_ASSETS = [
  './',
  './index.html',
  './calendar.html',
  './forecast-map.html',
  './quiz.html',
  './widget.html',
  './quizzes/engine.js',
  // ... more entries ...
  './shared-nav.js',
  './today-page.js',
  './compare.html',
  './compare-page.js',
  // ...
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update service worker with new assets and bump CACHE_VERSION</name>
  <files>custard-calendar/docs/sw.js</files>
  <action>
1. Bump CACHE_VERSION from 'custard-v13' to 'custard-v14'.

2. Add the following entries to the STATIC_ASSETS array:
   - './fun.html'
   - './fun-page.js'
   - './updates.html'
   - './updates-page.js'

   Place them after the existing compare entries (after './compare-page.js') to keep alphabetical-ish grouping.

3. Do NOT remove any existing entries. Old pages (calendar.html, widget.html, etc.) remain in STATIC_ASSETS since they are still accessible by direct URL.

4. Do NOT modify the install, activate, or fetch event handlers. Only the CACHE_VERSION and STATIC_ASSETS constants change.
  </action>
  <verify>
    <automated>grep -c 'custard-v14' /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/docs/sw.js && grep -c 'fun.html\|updates.html\|fun-page.js\|updates-page.js' /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/docs/sw.js</automated>
  </verify>
  <done>CACHE_VERSION is 'custard-v14'. STATIC_ASSETS includes fun.html, fun-page.js, updates.html, updates-page.js.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 4 delivery</name>
  <files></files>
  <action>
Run the full browser test suite, then present visual evidence (Playwright screenshots or serve the site) for user review at 375px viewport.

Before presenting to user, run:
```bash
cd custard-calendar/worker && npm run test:browser -- --workers=1
```

All tests must pass green before requesting visual verification.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npm run test:browser -- --workers=1 2>&1 | tail -5</automated>
  </verify>
  <done>Full browser test suite passes green. User approves visual verification of: 4-item nav on all pages, shared footer, fun page cards, updates page sections, quiz mode auto-select, and CTA links at 375px viewport.</done>
</task>

</tasks>

<verification>
Full browser test suite:
```bash
cd custard-calendar/worker && npm run test:browser -- --workers=1
```

Individual test files:
```bash
cd custard-calendar/worker && npx playwright test --config playwright.config.mjs test/browser/nav-clickthrough.spec.mjs test/browser/nav-footer.spec.mjs test/browser/nav-375px.spec.mjs test/browser/fun-page.spec.mjs test/browser/updates-page.spec.mjs --workers=1
```

Visual verification checklist:
1. Nav on index.html: 4 items (Today, Compare, Map, Fun). "Today" active. No overflow at 375px. Footer with "Get Updates" and "Privacy" links.
2. Nav on compare.html: Same 4 items. "Compare" active. Footer visible. "Want this every day?" CTA linking to updates.html.
3. fun.html: 6 quiz mode cards, Mad Libs section, Group Vote card to group.html, Fronts card to forecast-map.html. "Fun" active in nav.
4. Tap Weather quiz card: navigates to quiz.html?mode=weather-v1, Weather quiz pre-selected.
5. updates.html: 4 stacked sections (Calendar, Flavor Alerts, Widget, Siri). Store auto-filled. Email input and flavor chips visible.
6. Old page (quiz.html): Still loads, 4-item nav, footer visible.
</verification>

<success_criteria>
- Full browser test suite passes green (all existing + new tests)
- Service worker CACHE_VERSION bumped to v14
- All new assets in STATIC_ASSETS
- Visual verification at 375px confirms: 4-item nav, footer, fun page cards, updates page sections
- User approves visual checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-pages-nav-activation/04-04-SUMMARY.md`
</output>
