# Milestone v1.1: Production Launch + Polish

**Status:** SHIPPED 2026-03-09
**Phases:** 6-8
**Total Plans:** 4

## Overview

Ship the v1.0 build to production and close out remaining active requirements (design token consumption, quiz visual differentiation).

## Phases

### Phase 6: CSS + Quiz Polish

**Goal**: Every CSS rule uses the design token system and quiz modes are visually distinct
**Depends on**: Phase 5 (design tokens defined in v1.0)
**Plans**: 2 plans

Plans:
- [x] 06-01: Extend :root tokens, create test scaffold, tokenize style.css colors and spacing
- [x] 06-02: Tokenize inline styles in fun.html/updates.html/quiz.html, add quiz mode visual differentiation

**Details:**
- Extended :root from 24 to 37 tokens with 13 new gap-filling tokens
- Replaced 184 lines of hardcoded hex colors, 220 lines of hardcoded spacing with token references
- Created test_design_tokens.py with selector-context-aware CSS static analysis
- Eliminated all 9 inline style="" attributes across fun.html, updates.html, quiz.html
- Added 6 quiz mode color theme declarations using data-quiz-mode attribute selectors
- Converted Mad Libs chip rendering from inline style.cssText to CSS class-based approach

### Phase 7: Production Deploy

**Goal**: The site is live at custard.chriskaschner.com and verified working
**Depends on**: Phase 6
**Plans**: 1 plan

Plans:
- [x] 07-01: Push 49 commits to origin/main, verify GitHub Pages rebuild, smoke test live site

**Details:**
- Pushed 49 commits (v1.0 restructure + v1.1 polish) to origin/main
- All 5 nav pages return HTTP 200 on live domain
- Worker API verified returning valid JSON
- Human verified end-to-end navigation
- No Worker redeployment needed (source unchanged)

### Phase 8: Quiz Mode Visual Differentiation

**Goal**: Each quiz mode has distinct visual styling so users can instantly tell which mode they're in
**Depends on**: Phase 6 (design tokens), Phase 7 (production deploy)
**Gap Closure:** Closes gaps from v1.1 audit (QUIZ-01)
**Plans**: 1 plan

Plans:
- [x] 08-01: Implement data-quiz-mode JS/CSS wiring, per-mode accent colors, fun.html card accents, static analysis test

**Details:**
- engine.js sets data-quiz-mode attribute on body at init and variant change
- quiz.html has 7 [data-quiz-mode] CSS selectors with unique accent/tint colors per mode
- fun.html quiz cards have 7 per-mode accent border-left colors
- Static analysis test verifies full wiring chain

---

## Milestone Summary

**Key Decisions:**
- Selector-context-aware CSS test parsing (enables accurate domain-specific detection)
- In-between spacing values left hardcoded to keep 6-step scale clean
- Domain-specific colors explicitly excluded from token requirement via allowed-list
- Quiz-specific palette colors mapped to quiz tokens rather than main tokens
- CSS color-mix used for derived accent shades
- No Worker redeployment needed (source unchanged across 49 commits)
- Curl-based smoke testing to bypass service worker cache

**Issues Resolved:**
- Design tokens defined but not consumed in v1.0 -- fully consumed in Phase 6
- Quiz mode visual differentiation gap -- closed by Phase 8

**Issues Deferred:**
- Hero cone PNGs cover 40/176 flavors (SVG fallback for rest)
- stores.json not in SW pre-cache list
- planner-shared.js is a 1,624-line untested monolith
- Mad Libs chip CSS classes have no CSS definitions (inline styles handle rendering)
- Compare page multi-store comparison broken (switches stores instead of side-by-side)

**Technical Debt Incurred:**
- Mad Libs chip rendering still uses inline styles despite CSS classes being assigned
- Phase 8 commits need pushing to deploy quiz visual differentiation to live site
- CI Repo Structure Check fails (.planning/ not in REPO_CONTRACT.md)

---

_For current project status, see .planning/ROADMAP.md_
