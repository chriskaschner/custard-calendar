---
phase: 07-production-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [DEPL-01, DEPL-02, DEPL-03]

must_haves:
  truths:
    - "custard.chriskaschner.com serves the v1.1 site content (not pre-restructure content)"
    - "All five nav pages return HTTP 200 on the live domain"
    - "Worker API at /api/v1/today returns valid JSON from the live domain"
    - "CI passes after the push (worker-tests, python-tests, repo-structure)"
  artifacts:
    - path: "custard-calendar (remote)"
      provides: "49 commits pushed to origin/main"
  key_links:
    - from: "origin/main"
      to: "GitHub Pages"
      via: "auto-rebuild on push"
      pattern: "gh api repos/chriskaschner/custard-calendar/pages/builds/latest"
    - from: "custard.chriskaschner.com"
      to: "/api/v1/*"
      via: "Cloudflare Worker (already deployed)"
      pattern: "curl.*api/v1/today"
---

<objective>
Push the completed v1.0 + v1.1 codebase to production and verify the live site works end-to-end.

Purpose: Ship all 49 unpushed commits to origin/main, triggering GitHub Pages to rebuild the live site at custard.chriskaschner.com. Then verify every page loads and the Worker API responds correctly.

Output: Live production site serving all v1.0 and v1.1 changes, verified via smoke tests.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-production-deploy/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-push validation and push to production</name>
  <files></files>
  <action>
Run pre-push validation inside the custard-calendar subdirectory, then push to remote.

IMPORTANT: The actual git repo with the remote is at `custard-calendar/` (a subdirectory of the working directory), NOT the root working directory.

Step 1 -- Run Worker unit tests:
  cd custard-calendar/worker && npm test
  Expected: 810+ tests passing in ~3.4 seconds.

Step 2 -- Run Python static asset and design token tests:
  cd custard-calendar && uv run pytest tests/test_static_assets.py tests/test_design_tokens.py -v
  Expected: All tests pass.

Step 3 -- Verify no sensitive files in the diff:
  cd custard-calendar && git diff --name-only origin/main..HEAD | grep -iE '\.env|credentials|secret|config\.yaml' || echo "No sensitive files found"
  Expected: "No sensitive files found"

Step 4 -- Push the 49 commits:
  cd custard-calendar && git push origin main
  Expected: Successful push of 49 commits.

Step 5 -- Wait for GitHub Pages to rebuild (poll up to 5 minutes):
  Poll build status with: gh api repos/chriskaschner/custard-calendar/pages/builds/latest --jq '.status'
  Wait until status is "built". Check every 15 seconds, up to 20 attempts.

Step 6 -- Verify CI triggered:
  cd custard-calendar && gh run list --repo chriskaschner/custard-calendar --limit 1
  Expected: A workflow run triggered by the push.

Do NOT run Playwright browser tests as a pre-push gate -- they require a local HTTP server and take 60+ seconds. The Worker unit tests and Python tests are sufficient pre-push validation. Playwright tests run in CI.

Do NOT run `wrangler deploy` -- no worker/src/ files changed in these 49 commits. The Worker was last deployed 2026-03-01 and does not need redeployment.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && git log --oneline origin/main..HEAD | wc -l | tr -d ' '</automated>
  </verify>
  <done>git log --oneline origin/main..HEAD returns 0 (no commits ahead of remote). GitHub Pages build status is "built".</done>
</task>

<task type="auto">
  <name>Task 2: Post-deploy smoke tests against live site</name>
  <files></files>
  <action>
Run HTTP smoke tests against the live production site to verify all pages and the API respond correctly.

Step 1 -- Check each nav page returns HTTP 200:
  for page in "" "compare.html" "map.html" "fun.html" "updates.html"; do
    STATUS=$(curl -sI -o /dev/null -w '%{http_code}' "https://custard.chriskaschner.com/$page")
    echo "$page: $STATUS"
  done
  Expected: All 5 pages return HTTP 200. If any return 404, the push may not have completed yet -- wait and retry.

Step 2 -- Check Worker API returns valid JSON:
  curl -s "https://custard.chriskaschner.com/api/v1/today?slug=mt-horeb" | python3 -m json.tool > /dev/null && echo "API OK" || echo "API FAIL"
  Expected: "API OK"

Step 3 -- Check the site serves NEW content (not pre-restructure):
  curl -s "https://custard.chriskaschner.com/" | grep -o 'shared-nav.js' | head -1
  Expected: "shared-nav.js" found in page source (this file only exists in v1.0+ restructure).

Step 4 -- Check HTTPS is enforced:
  curl -sI "http://custard.chriskaschner.com/" | head -1
  Expected: HTTP redirect (301 or 302) to HTTPS, or direct HTTPS response.

IMPORTANT: Use curl for all checks, NOT a browser. Service worker caching can show stale content in browsers. Curl hits the origin directly.

If any page returns 404 but the push succeeded, wait 60 seconds and retry -- GitHub Pages may still be rebuilding.
  </action>
  <verify>
    <automated>STATUS=$(curl -sI -o /dev/null -w '%{http_code}' "https://custard.chriskaschner.com/compare.html") && [ "$STATUS" = "200" ] && echo "PASS" || echo "FAIL: got $STATUS"</automated>
  </verify>
  <done>All 5 nav pages (index, compare, map, fun, updates) return HTTP 200. Worker API at /api/v1/today returns valid JSON. Site source contains shared-nav.js (v1.0+ content confirmed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of live site navigation</name>
  <action>
Present the live site to the user for manual end-to-end verification. This checkpoint confirms DEPL-03 (user can navigate all pages without errors) by having a human walk through the complete site.
  </action>
  <verify>User confirms the live site works correctly by navigating all pages.</verify>
  <done>User types "approved" after verifying all pages load and function correctly on the live site.</done>
  <what-built>Complete production deployment of custard.chriskaschner.com with all v1.0 and v1.1 changes (49 commits pushed, GitHub Pages rebuilt, smoke tests passed).</what-built>
  <how-to-verify>
    1. Open https://custard.chriskaschner.com/ in a browser (incognito recommended to bypass SW cache)
    2. Verify Today page loads with flavor cards, store indicator, and shared navigation
    3. Click "Compare" in nav -- verify Compare page loads with day-first card grid
    4. Click "Map" in nav -- verify Map page loads with forecast map
    5. Click "Fun" in nav -- verify Fun page loads with quiz mode cards
    6. Scroll to footer and click "Get Updates" -- verify Updates page loads
    7. On Fun page, try starting a quiz -- verify quiz mode has visual differentiation (unique color treatment per mode)
    8. Check that the store indicator (top of page) shows a store and responds to selection
  </how-to-verify>
  <resume-signal>Type "approved" if the live site works correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
All three deployment requirements verified:
- DEPL-01: custard.chriskaschner.com serves v1.1 content (curl confirms shared-nav.js in source)
- DEPL-02: Worker API responds with valid JSON (curl confirms /api/v1/today returns JSON)
- DEPL-03: All nav pages load without errors (HTTP 200 for all 5 pages + human verification of full navigation flow)
</verification>

<success_criteria>
1. Zero commits ahead of origin/main (all 49 pushed)
2. All 5 pages return HTTP 200 from custard.chriskaschner.com
3. /api/v1/today returns valid JSON
4. Human confirms live site navigation works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/07-production-deploy/07-01-SUMMARY.md`
</output>
