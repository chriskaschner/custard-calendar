---
phase: 11-monolith-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custard-calendar/docs/planner-shared.js
  - custard-calendar/docs/planner-data.js
  - custard-calendar/docs/planner-domain.js
  - custard-calendar/docs/planner-ui.js
  - custard-calendar/worker/test/browser/api-surface.spec.mjs
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "planner-shared.js is reduced from 1,639 lines to ~200 lines containing only facade + core utilities"
    - "planner-data.js exists with brand constants, normalize, haversine, similarity groups, flavor families, seasonal detection"
    - "planner-domain.js exists with certainty, timeline, rarity, reliability, store persistence, drive preferences, historical context"
    - "planner-ui.js exists with action CTAs, signals, share button, telemetry"
    - "All 60 named exports are present across the 4 files with no missing symbols"
    - "API surface smoke test exists and passes"
  artifacts:
    - path: "custard-calendar/docs/planner-shared.js"
      provides: "Facade IIFE creating window.CustardPlanner with core utils + underscore-prefixed internal helpers"
      contains: "var CustardPlanner"
    - path: "custard-calendar/docs/planner-data.js"
      provides: "Brand, normalize, haversine, families, seasonal sub-module"
      contains: "Object.assign(CP"
    - path: "custard-calendar/docs/planner-domain.js"
      provides: "Certainty, timeline, rarity, reliability, store/drive state, history sub-module"
      contains: "Object.assign(CP"
    - path: "custard-calendar/docs/planner-ui.js"
      provides: "CTAs, signals, share, telemetry sub-module"
      contains: "Object.assign(CP"
    - path: "custard-calendar/worker/test/browser/api-surface.spec.mjs"
      provides: "Playwright test checking all 60 exports on window.CustardPlanner"
      contains: "EXPECTED_KEYS"
  key_links:
    - from: "custard-calendar/docs/planner-data.js"
      to: "window.CustardPlanner"
      via: "Object.assign extending facade object"
      pattern: "Object\\.assign\\(CP"
    - from: "custard-calendar/docs/planner-domain.js"
      to: "window.CustardPlanner"
      via: "Object.assign extending facade object"
      pattern: "Object\\.assign\\(CP"
    - from: "custard-calendar/docs/planner-ui.js"
      to: "window.CustardPlanner"
      via: "Object.assign extending facade object"
      pattern: "Object\\.assign\\(CP"
    - from: "custard-calendar/docs/planner-domain.js"
      to: "custard-calendar/docs/planner-shared.js"
      via: "CP._normalizeStringList, CP._buildStoreLookup, CP._parseLegacySecondaryStores, CP._PRIMARY_STORE_KEY etc."
      pattern: "CP\\._"
---

<objective>
Split planner-shared.js (1,639 lines) into a slim facade + 3 sub-modules preserving the exact 60-export public API on window.CustardPlanner.

Purpose: Enable targeted browser caching per module and create maintainable code boundaries. This is the core extraction work for ARCH-01.
Output: 4 JS files (1 slimmed facade + 3 new sub-modules) + 1 API surface Playwright test.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monolith-refactoring/11-CONTEXT.md
@.planning/phases/11-monolith-refactoring/11-RESEARCH.md

@custard-calendar/docs/planner-shared.js
@custard-calendar/docs/sw.js
@custard-calendar/worker/playwright.config.mjs

<interfaces>
<!-- The current monolith's public API -- all 60 exports from the return {} block (lines 1409-1495).
     Every key listed here MUST appear either in the facade's return {} or in exactly one sub-module's Object.assign. -->

Public API (60 exports on window.CustardPlanner):
  Facade keeps: WORKER_BASE, escapeHtml
  Facade exposes for sub-modules (underscore-prefixed):
    _normalizeStringList, _buildStoreLookup, _parseLegacySecondaryStores,
    _PRIMARY_STORE_KEY, _SECONDARY_STORES_KEY, _FAVORITES_KEY, _MAX_FAVORITES,
    _DRIVE_PREFERENCES_KEY, _DRIVE_PREF_VERSION,
    _DRIVE_ALLOWED_EXCLUDES, _DRIVE_ALLOWED_TAGS, _DRIVE_ALLOWED_SORTS

  planner-data.js adds: BRAND_COLORS, BRAND_DISPLAY, brandFromSlug, brandDisplayName,
    normalize, haversineMiles, SIMILARITY_GROUPS, FLAVOR_FAMILIES, FLAVOR_FAMILY_MEMBERS,
    getFamilyForFlavor, getFamilyColor, findSimilarFlavors, findSimilarToFavorites,
    isSeasonalFlavor

  planner-domain.js adds: CERTAINTY, CERTAINTY_LABELS, certaintyTier, certaintyBadgeHTML,
    certaintyCardClass, confidenceStripClass, buildTimeline,
    fetchReliability, watchBannerHTML,
    fetchFlavorHistoricalContext, fetchStoreHistoricalContext, historicalContextHTML,
    getPrimaryStoreSlug, setPrimaryStoreSlug, getSavedStore, setSavedStore,
    getFavorites, addFavorite, removeFavorite,
    getDrivePreferences, saveDrivePreferences, flushDrivePreferences, resetDrivePreferences,
    pickDefaultDriveStores, parseDriveUrlState, buildDriveUrlState,
    DRIVE_PREFERENCES_KEY, DRIVE_DEBOUNCE_MS,
    rarityLabelFromGapDays, rarityLabelFromPercentile, rarityLabelFromRank, formatCadenceText

  planner-ui.js adds: directionsUrl, calendarIcsUrl, alertPageUrl, actionCTAsHTML,
    emitInteractionEvent, emitPageView, getPageLoadId,
    signalCardHTML, fetchSignals, initShareButton
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API surface smoke test and extract 3 sub-modules + slim facade</name>
  <files>
    custard-calendar/worker/test/browser/api-surface.spec.mjs,
    custard-calendar/docs/planner-shared.js,
    custard-calendar/docs/planner-data.js,
    custard-calendar/docs/planner-domain.js,
    custard-calendar/docs/planner-ui.js
  </files>
  <action>
**Step 1 -- Create API surface smoke test (Wave 0 gap):**
Create `worker/test/browser/api-surface.spec.mjs` with the EXPECTED_KEYS array containing all 60 public exports (listed in RESEARCH.md "Complete Public API Catalog" section). The test navigates to `/index.html` and checks that every key in EXPECTED_KEYS exists on `window.CustardPlanner`. Use the exact Playwright pattern from RESEARCH.md code example. Run it against the CURRENT (unsplit) monolith first to verify it passes -- this baseline proves the test is correct before the refactor begins.

**Step 2 -- Extract planner-data.js (~500 lines):**
Create `docs/planner-data.js` as a new IIFE. Move these sections from planner-shared.js:
- Brand constants and functions: BRAND_COLORS, BRAND_DISPLAY, brandFromSlug, brandDisplayName (lines ~739-769)
- Normalize function (lines ~777-785)
- Haversine function (lines ~791-800)
- Similarity groups and flavor families: SIMILARITY_GROUPS, FLAVOR_FAMILIES, FLAVOR_FAMILY_MEMBERS, getFamilyForFlavor, getFamilyColor, findSimilarFlavors, findSimilarToFavorites (lines ~803-984)
- Seasonal detection: isSeasonalFlavor (lines ~43-46) -- move to data since it's about flavor categorization
- Private helpers that ONLY data uses: syncMutableObject, normalizeGroupMembers, normalizeFamilyConfig, normalizeSimilarityConfig, applyFlavorConfig, rebuildFlavorFamilyIndexes, bootstrapFlavorConfig (lines ~855-968)
- Load-time side effects that belong here: the `rebuildFlavorFamilyIndexes()` and `bootstrapFlavorConfig()` calls (line ~967-968)

Pattern: `(function () { 'use strict'; var CP = window.CustardPlanner; if (!CP) { console.error('planner-data.js: CustardPlanner not found'); return; } ... Object.assign(CP, { ... }); })();`

ALL function definitions MUST appear BEFORE the Object.assign call (no function hoisting across IIFEs). Cross-module references use `CP.escapeHtml()` etc. via the facade global.

**Step 3 -- Extract planner-domain.js (~400 lines):**
Create `docs/planner-domain.js` as a new IIFE. Move these sections:
- Certainty vocabulary: CERTAINTY, CERTAINTY_LABELS, certaintyTier, certaintyBadgeHTML, certaintyCardClass, confidenceStripClass (lines ~987-1045)
- Timeline building: buildTimeline (lines ~1046-1115)
- Rarity labels: rarityLabelFromGapDays, rarityLabelFromPercentile, rarityLabelFromRank, formatCadenceText (lines ~436-471)
- Reliability: fetchReliability, watchBannerHTML (lines ~1116-1200)
- Historical context: fetchFlavorHistoricalContext, fetchStoreHistoricalContext, historicalContextHTML + private helper formatInt (lines ~1265-1397)
- Store persistence: getPrimaryStoreSlug, setPrimaryStoreSlug, getSavedStore, setSavedStore, getFavorites, addFavorite, removeFavorite (lines ~473-545)
- Drive preferences: ALL drive-related functions (pickDefaultDriveStores, makeDefaultDrivePreferences, sanitizeDrivePreferences, parseDriveUrlState, applyDriveUrlState, getDrivePreferences, saveDrivePreferences, flushDrivePreferences, resetDrivePreferences, buildDriveUrlState) + private drive helpers (isCulversSlug, sanitizeDriveStores, sanitizeDriveTagList, parseCsvTagParam, sanitizeDriveSort, sanitizeDriveRadius, _writeDrivePrefsToStorage, _debounceSaveTimer, _lastSavedPrefs) (lines ~103-435)
- DRIVE_DEBOUNCE_MS constant (line ~353)
- Load-time side effect: `window.addEventListener('beforeunload', flushDrivePreferences)` (lines ~1401-1403) -- stays with drive code
- Private mutable state: _flavorContextCache, _storeContextCache stay with history functions

Access facade internals via: `CP._normalizeStringList`, `CP._buildStoreLookup`, `CP._parseLegacySecondaryStores`, `CP._PRIMARY_STORE_KEY`, `CP._SECONDARY_STORES_KEY`, `CP._FAVORITES_KEY`, `CP._MAX_FAVORITES`, `CP._DRIVE_PREFERENCES_KEY`, `CP._DRIVE_PREF_VERSION`, `CP._DRIVE_ALLOWED_EXCLUDES`, `CP._DRIVE_ALLOWED_TAGS`, `CP._DRIVE_ALLOWED_SORTS`.

**Step 4 -- Extract planner-ui.js (~500 lines):**
Create `docs/planner-ui.js` as a new IIFE. Move these sections:
- Action CTAs: directionsUrl, calendarIcsUrl, alertPageUrl, actionCTAsHTML + private helper ctaDataAttrs (lines ~1202-1264)
- Telemetry: emitInteractionEvent, emitPageView, getPageLoadId + all private telemetry helpers (inferPageKey, makePageLoadId, cleanTelemetryText, cleanTelemetrySlug, cleanTelemetryEventType, cleanTelemetryAction, cleanTelemetryCertainty, cleanTelemetryDeviceType, inferCtaAction, bindInteractionTelemetry) + private state (_pageLoadId, _telemetryListenerBound) (lines ~548-733)
- Signals: signalCardHTML, trackSignalViews, fetchSignalsShared (lines ~1497-1598, currently AFTER the return statement due to hoisting -- must be placed BEFORE Object.assign in the new file)
- Share button: initShareButton (lines ~1600-1638, also after return statement)
- Load-time side effects: `bindInteractionTelemetry()` call and auto-`emitPageView` DOMContentLoaded block (lines ~703-733) -- stay with telemetry code

CRITICAL hoisting pitfall: signalCardHTML, trackSignalViews, fetchSignalsShared, and initShareButton are currently defined AFTER the `return {}` block (lines 1497-1639) and work only because JavaScript hoists function declarations. In the new IIFE they MUST be defined BEFORE the Object.assign call.

Note: fetchSignals is an alias for fetchSignalsShared in the public API (`fetchSignals: fetchSignalsShared` in the return block).

**Step 5 -- Slim down planner-shared.js facade (~200 lines):**
Reduce planner-shared.js to contain only:
- The opening `var CustardPlanner = (function () { 'use strict';` wrapper
- WORKER_BASE constant
- localStorage key constants (PRIMARY_STORE_KEY, SECONDARY_STORES_KEY, FAVORITES_KEY, MAX_FAVORITES, DRIVE_PREFERENCES_KEY, DRIVE_PREF_VERSION)
- Drive allowed maps (DRIVE_ALLOWED_EXCLUDES, DRIVE_ALLOWED_TAGS, DRIVE_ALLOWED_SORTS)
- Core utility functions: escapeHtml, normalizeStringList, parseLegacySecondaryStores, buildStoreLookup
- The return {} block exposing: WORKER_BASE, escapeHtml as public; and underscore-prefixed internal helpers (_normalizeStringList, _buildStoreLookup, _parseLegacySecondaryStores, _PRIMARY_STORE_KEY, _SECONDARY_STORES_KEY, _FAVORITES_KEY, _MAX_FAVORITES, _DRIVE_PREFERENCES_KEY, _DRIVE_PREF_VERSION, _DRIVE_ALLOWED_EXCLUDES, _DRIVE_ALLOWED_TAGS, _DRIVE_ALLOWED_SORTS)
- The closing `})();`
- Update the JSDoc header to note that sub-modules extend this object

Remove ALL code that has been moved to the 3 sub-modules. Remove the load-time side effects that moved. Remove the functions after the return block (signals, share button).

**Step 6 -- Verify API surface test passes with the split files:**
The API surface test navigates to index.html which currently only loads planner-shared.js. At this point the HTML pages have NOT been updated yet (that is Plan 02). So to verify during development: temporarily add the 3 new script tags to index.html for the test run, confirm the API surface test passes, then revert index.html. Alternatively, create a minimal test harness HTML page that loads all 4 scripts and run the test against that. Document whichever approach is used.

IMPORTANT: Do NOT permanently update HTML pages in this plan -- that is Plan 02's scope. The verification here is to ensure the split is correct before wiring.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx playwright test test/browser/api-surface.spec.mjs --workers=1</automated>
  </verify>
  <done>
    - planner-shared.js is ~200 lines (down from 1,639)
    - planner-data.js, planner-domain.js, planner-ui.js exist as self-contained IIFEs
    - Each sub-module opens with guard check for window.CustardPlanner
    - Each sub-module extends CustardPlanner via Object.assign
    - All 60 public API exports are accounted for across the 4 files
    - API surface smoke test passes (all EXPECTED_KEYS found on window.CustardPlanner)
    - No function definitions appear after Object.assign in any sub-module
  </done>
</task>

</tasks>

<verification>
1. API surface smoke test passes: `cd custard-calendar/worker && npx playwright test test/browser/api-surface.spec.mjs --workers=1`
2. File count: 4 JS files exist in docs/ (planner-shared.js, planner-data.js, planner-domain.js, planner-ui.js)
3. Line count: `wc -l custard-calendar/docs/planner-shared.js` should show ~200 lines (not 1,639)
4. No missing exports: Every key from the return block appears in exactly one file's Object.assign or facade return
</verification>

<success_criteria>
- planner-shared.js reduced to facade-only (~200 lines)
- 3 new sub-module files created following IIFE + Object.assign pattern
- API surface Playwright test exists and passes confirming all 60 exports present
- No code duplication between files (each export lives in exactly one place)
</success_criteria>

<output>
After completion, create `.planning/phases/11-monolith-refactoring/11-01-SUMMARY.md`
</output>
