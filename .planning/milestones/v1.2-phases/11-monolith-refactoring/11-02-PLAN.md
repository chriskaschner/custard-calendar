---
phase: 11-monolith-refactoring
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - custard-calendar/docs/index.html
  - custard-calendar/docs/map.html
  - custard-calendar/docs/compare.html
  - custard-calendar/docs/fun.html
  - custard-calendar/docs/updates.html
  - custard-calendar/docs/quiz.html
  - custard-calendar/docs/forecast-map.html
  - custard-calendar/docs/group.html
  - custard-calendar/docs/privacy.html
  - custard-calendar/docs/sw.js
autonomous: true
requirements: [ARCH-01, ARCH-02]

must_haves:
  truths:
    - "Every page that loads planner-shared.js also loads all 3 sub-modules in the correct order"
    - "All 31 existing Playwright browser tests pass with zero regressions"
    - "New JS files are in SW STATIC_ASSETS and CACHE_VERSION is bumped"
    - "Consumer code (CustardPlanner.escapeHtml etc.) works without any changes to page-specific JS"
  artifacts:
    - path: "custard-calendar/docs/index.html"
      provides: "Updated script tags loading all 4 planner modules"
      contains: "planner-data.js"
    - path: "custard-calendar/docs/sw.js"
      provides: "Updated STATIC_ASSETS with 3 new files and bumped CACHE_VERSION"
      contains: "custard-v18"
  key_links:
    - from: "custard-calendar/docs/index.html"
      to: "custard-calendar/docs/planner-data.js"
      via: "script tag after planner-shared.js"
      pattern: "planner-data\\.js"
    - from: "custard-calendar/docs/index.html"
      to: "custard-calendar/docs/planner-domain.js"
      via: "script tag after planner-data.js"
      pattern: "planner-domain\\.js"
    - from: "custard-calendar/docs/index.html"
      to: "custard-calendar/docs/planner-ui.js"
      via: "script tag after planner-domain.js"
      pattern: "planner-ui\\.js"
    - from: "custard-calendar/docs/sw.js"
      to: "custard-calendar/docs/planner-data.js"
      via: "STATIC_ASSETS array entry"
      pattern: "planner-data\\.js"
---

<objective>
Wire the 3 new sub-modules into all 9 HTML pages and update the service worker, then run the full Playwright regression suite to gate ARCH-02.

Purpose: Complete the monolith refactoring by connecting the extracted modules to every consuming page and ensuring zero regressions across all 31 browser tests.
Output: 9 updated HTML files + updated sw.js + full green test suite.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-monolith-refactoring/11-CONTEXT.md
@.planning/phases/11-monolith-refactoring/11-RESEARCH.md
@.planning/phases/11-monolith-refactoring/11-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: the 4 JS files and their load order -->
Script load order on every page:
  1. planner-shared.js  (facade, creates window.CustardPlanner)
  2. planner-data.js    (extends CP with brand/normalize/families)
  3. planner-domain.js  (extends CP with certainty/store/drive)
  4. planner-ui.js      (extends CP with CTAs/signals/telemetry)

Pages requiring updates (from RESEARCH.md verified locations):
  index.html:146, map.html:100, compare.html:83, fun.html:219,
  updates.html:239, quiz.html:587, forecast-map.html:142,
  group.html:293, privacy.html:73

SW current state:
  CACHE_VERSION = 'custard-v17'
  STATIC_ASSETS includes './planner-shared.js' at line 12
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sub-module script tags to all 9 HTML pages</name>
  <files>
    custard-calendar/docs/index.html,
    custard-calendar/docs/map.html,
    custard-calendar/docs/compare.html,
    custard-calendar/docs/fun.html,
    custard-calendar/docs/updates.html,
    custard-calendar/docs/quiz.html,
    custard-calendar/docs/forecast-map.html,
    custard-calendar/docs/group.html,
    custard-calendar/docs/privacy.html
  </files>
  <action>
For each of the 9 HTML pages listed below, insert 3 new script tags immediately after the existing `<script src="planner-shared.js"></script>` line. The insertion order MUST be: planner-data.js, planner-domain.js, planner-ui.js. All 3 scripts load on every page (no selective loading -- per CONTEXT.md decision).

The exact pattern to insert after the planner-shared.js line:
```html
<script src="planner-data.js"></script>
<script src="planner-domain.js"></script>
<script src="planner-ui.js"></script>
```

Pages and their planner-shared.js line locations (verified from RESEARCH.md):
1. **index.html** (line 146): Insert after `<script src="planner-shared.js"></script>`, before `<script src="shared-nav.js"></script>`
2. **map.html** (line 100): Insert after planner-shared.js, before shared-nav.js
3. **compare.html** (line 83): Insert after planner-shared.js, before shared-nav.js
4. **fun.html** (line 219): Insert after planner-shared.js, before shared-nav.js
5. **updates.html** (line 239): Insert after planner-shared.js, before shared-nav.js
6. **quiz.html** (line 587): Insert after planner-shared.js, before shared-nav.js
7. **forecast-map.html** (line 142): Insert after planner-shared.js, before shared-nav.js
8. **group.html** (line 293): Insert after planner-shared.js, before shared-nav.js
9. **privacy.html** (line 73): Insert after planner-shared.js, before shared-nav.js

IMPORTANT: Do NOT modify any other part of the HTML files. Only add the 3 script tags. Consumer code (anything calling CustardPlanner.xyz) requires zero changes per the facade strategy.

After all 9 files are updated, verify with a quick grep:
`grep -l 'planner-data.js' custard-calendar/docs/*.html | wc -l` should return 9.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard && grep -c 'planner-data.js' custard-calendar/docs/index.html custard-calendar/docs/map.html custard-calendar/docs/compare.html custard-calendar/docs/fun.html custard-calendar/docs/updates.html custard-calendar/docs/quiz.html custard-calendar/docs/forecast-map.html custard-calendar/docs/group.html custard-calendar/docs/privacy.html</automated>
  </verify>
  <done>All 9 HTML pages include planner-data.js, planner-domain.js, planner-ui.js script tags in the correct order immediately after planner-shared.js</done>
</task>

<task type="auto">
  <name>Task 2: Update SW STATIC_ASSETS and bump CACHE_VERSION, then run full regression suite</name>
  <files>custard-calendar/docs/sw.js</files>
  <action>
**Step 1 -- Update sw.js:**
- Bump CACHE_VERSION from `'custard-v17'` to `'custard-v18'`
- Add 3 new entries to the STATIC_ASSETS array immediately after the existing `'./planner-shared.js'` entry (line 12):
  - `'./planner-data.js'`
  - `'./planner-domain.js'`
  - `'./planner-ui.js'`
- Do NOT change anything else in sw.js

**Step 2 -- Run API surface smoke test:**
`cd custard-calendar/worker && npx playwright test test/browser/api-surface.spec.mjs --workers=1`
This must pass (all 60 exports present on window.CustardPlanner).

**Step 3 -- Run FULL Playwright browser regression suite:**
`cd custard-calendar/worker && npm run test:browser -- --workers=1`
All 31 existing browser tests MUST pass. This is the hard gate for ARCH-02.

If any test fails:
- Read the failure output carefully
- The most likely causes are: (a) missing export in Object.assign, (b) function hoisting issue in planner-ui.js, (c) private state split across wrong modules, (d) script tag ordering error in HTML
- Fix the root cause in the appropriate file (planner-shared.js, planner-data.js, planner-domain.js, planner-ui.js, or HTML pages)
- Re-run the full suite until all tests pass

**Step 4 -- Verify file sizes are reasonable:**
Run `wc -l custard-calendar/docs/planner-shared.js custard-calendar/docs/planner-data.js custard-calendar/docs/planner-domain.js custard-calendar/docs/planner-ui.js` to confirm the split is roughly in the expected range (~200 + ~500 + ~400 + ~500 lines).
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npm run test:browser -- --workers=1</automated>
  </verify>
  <done>
    - sw.js CACHE_VERSION is 'custard-v18'
    - sw.js STATIC_ASSETS includes planner-data.js, planner-domain.js, planner-ui.js
    - API surface smoke test passes (all 60 exports present)
    - All 31+ existing Playwright browser tests pass with zero regressions (ARCH-02 satisfied)
  </done>
</task>

</tasks>

<verification>
1. All HTML pages wired: `grep -l 'planner-data.js' custard-calendar/docs/*.html | wc -l` returns 9
2. SW updated: `grep 'custard-v18' custard-calendar/docs/sw.js` returns a match
3. SW assets: `grep 'planner-data.js\|planner-domain.js\|planner-ui.js' custard-calendar/docs/sw.js | wc -l` returns 3
4. Full regression: `cd custard-calendar/worker && npm run test:browser -- --workers=1` all green
5. API surface: `cd custard-calendar/worker && npx playwright test test/browser/api-surface.spec.mjs --workers=1` passes
</verification>

<success_criteria>
- 9 HTML pages load all 4 planner modules in correct order
- SW pre-caches 3 new JS files with CACHE_VERSION bump to v18
- All 31+ existing Playwright browser tests pass (zero regressions)
- API surface smoke test confirms all 60 exports intact
- No changes required to any page-specific JS (today-page.js, compare-page.js, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/11-monolith-refactoring/11-02-SUMMARY.md`
</output>
