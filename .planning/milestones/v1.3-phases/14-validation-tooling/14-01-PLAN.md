---
phase: 14-validation-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custard-calendar/worker/package.json
  - custard-calendar/worker/test/helpers/render-to-pixels.js
  - custard-calendar/worker/test/flavor-colors.test.js
  - custard-calendar/worker/test/palette-sync.test.js
  - custard-calendar/worker/test/contrast-check.test.js
  - custard-tidbyt/apps/culversfotd/culvers_fotd.star
autonomous: true
requirements: [VALD-01, VALD-02]

must_haves:
  truths:
    - "CI test fails if any color hex drifts between flavor-colors.js and the 4 downstream sync files"
    - "CI test fails if any topping/base combination in FLAVOR_PROFILES falls below 3:1 contrast ratio"
    - "All profiled flavors pass contrast check before gate goes live (clean slate for Phase 16)"
    - "Starlark color drift in custard-tidbyt is fixed before sync gate activates"
  artifacts:
    - path: "custard-calendar/worker/test/helpers/render-to-pixels.js"
      provides: "Shared renderToPixels and pixelMapToRGBA utilities extracted from flavor-colors.test.js"
      exports: ["renderToPixels", "pixelMapToRGBA"]
    - path: "custard-calendar/worker/test/palette-sync.test.js"
      provides: "CI gate for color hex sync across 5 files"
      contains: "describe.*palette sync"
    - path: "custard-calendar/worker/test/contrast-check.test.js"
      provides: "CI gate for topping/base contrast ratios"
      contains: "describe.*contrast"
  key_links:
    - from: "custard-calendar/worker/test/palette-sync.test.js"
      to: "custard-calendar/worker/src/flavor-colors.js"
      via: "ESM import of canonical color dicts"
      pattern: "import.*BASE_COLORS.*from.*flavor-colors"
    - from: "custard-calendar/worker/test/palette-sync.test.js"
      to: "custard-calendar/docs/cone-renderer.js"
      via: "fs.readFileSync + regex parse of FALLBACK_ var declarations"
      pattern: "readFileSync.*cone-renderer"
    - from: "custard-calendar/worker/test/palette-sync.test.js"
      to: "custard-calendar/tidbyt/culvers_fotd.star"
      via: "fs.readFileSync + regex parse of Starlark dict literals"
      pattern: "readFileSync.*culvers_fotd"
    - from: "custard-calendar/worker/test/contrast-check.test.js"
      to: "custard-calendar/worker/src/flavor-colors.js"
      via: "ESM import of FLAVOR_PROFILES, BASE_COLORS, TOPPING_COLORS"
      pattern: "import.*FLAVOR_PROFILES.*from.*flavor-colors"
---

<objective>
Create CI guard tests for palette sync (VALD-01) and contrast checking (VALD-02). Install pixelmatch/pngjs dependencies needed by this plan and Plan 02. Extract renderToPixels to a shared test utility for reuse. Fix Starlark color drift in custard-tidbyt before enabling the sync gate.

Purpose: Automated guards prevent color drift and low-contrast toppings from reaching production before bulk profile authoring (Phase 16) begins.
Output: Two new Vitest test files that run as part of `npm test`, shared test utility, fixed Starlark colors.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-validation-tooling/14-CONTEXT.md
@.planning/phases/14-validation-tooling/14-RESEARCH.md
@.planning/phases/13-rendering-quality-fixes/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From custard-calendar/worker/src/flavor-colors.js (canonical source of truth):
```javascript
export const BASE_COLORS = { vanilla: '#F5DEB3', chocolate: '#6F4E37', /* ... 13 total keys */ };
export const RIBBON_COLORS = { caramel: '#D38B2C', peanut_butter: '#D4A017', marshmallow: '#FFFFFF', chocolate_syrup: '#1A0A00', fudge: '#3B1F0B' };
export const TOPPING_COLORS = { oreo: '#1A1A1A', andes: '#1FAE7A', dove: '#2B1A12', /* ... 20 total keys */ };
export const CONE_COLORS = { waffle: '#D2691E', waffle_dark: '#B8860B' };
export const FLAVOR_PROFILES = { /* 38 entries, each: { base, ribbon, toppings, density } */ };
export function renderConeSVG(flavorName, scale = 1);
export function renderConeHDSVG(flavorName, scale = 1);
export function renderConeHeroSVG(flavorName, scale = 1);
export function renderConePremiumSVG(flavorName, scale = 1);
```

From custard-calendar/worker/test/flavor-colors.test.js (lines 444-488, to be extracted):
```javascript
// renderToPixels: SVG string -> Map<"x,y", "#HEXCOLOR">
function renderToPixels(svgStr) { /* parses <rect> elements */ }
// stableHash: Map -> 8-char hex hash (djb2)
function stableHash(map) { /* sorts entries, JSON, djb2 */ }
```

Existing test structure uses TIER_SPECS array:
```javascript
const TIER_SPECS = [
  { name: 'Mini',    fn: renderConeSVG,        spec: MINI_SPEC },
  { name: 'HD',      fn: renderConeHDSVG,      spec: HD_SPEC },
  { name: 'Premium', fn: renderConePremiumSVG,  spec: PREMIUM_SPEC },
  { name: 'Hero',    fn: renderConeHeroSVG,     spec: HERO_SPEC },
];
```

Color dict format in downstream files:
- docs/cone-renderer.js: `var FALLBACK_BASE_COLORS = { vanilla: '#F5DEB3', ... }`
- tidbyt/culvers_fotd.star: `BASE_COLORS = { "vanilla": "#F5DEB3", ... }`
- custard-tidbyt/apps/culversfotd/culvers_fotd.star: same Starlark format
- docs/flavor-audit.html: `var SEED_BASE = { vanilla:'#F5DEB3', ... }` (single quotes)

NOTE: Starlark files do NOT have a named CONE_COLORS dict. Cone colors (#D2691E and #B8860B) appear as inline hex literals in render.Box() calls. Sync test for Starlark cone colors must verify these hex values appear in the file, not look for a named dict.

NOTE: SEED_CONE in flavor-audit.html: `var SEED_CONE = { waffle:'#D2691E', waffle_dark:'#B8860B' };`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install deps, extract shared utility, fix Starlark drift</name>
  <files>
    custard-calendar/worker/package.json,
    custard-calendar/worker/test/helpers/render-to-pixels.js,
    custard-calendar/worker/test/flavor-colors.test.js,
    custard-tidbyt/apps/culversfotd/culvers_fotd.star
  </files>
  <action>
    1. Install pixelmatch and pngjs in worker/:
       `cd custard-calendar/worker && npm install --save-dev pixelmatch pngjs`

    2. Create shared test utility at `worker/test/helpers/render-to-pixels.js`:
       - Extract `renderToPixels(svgStr)` function from flavor-colors.test.js (lines 444-469)
       - Extract `stableHash(map)` function from flavor-colors.test.js (lines 475-488)
       - Add new `pixelMapToRGBA(pixelMap, width, height)` function that converts a Map of "x,y" -> "#HEXCOLOR" to a Uint8Array RGBA buffer (4 bytes per pixel, 255 alpha for colored pixels, 0 for empty)
       - Export all three functions

    3. Update `worker/test/flavor-colors.test.js`:
       - Import `renderToPixels` and `stableHash` from `./helpers/render-to-pixels.js`
       - Remove the local function definitions (lines 444-488)
       - All existing tests must still pass unchanged

    4. Fix custard-tidbyt Starlark color drift. The file at `custard-tidbyt/apps/culversfotd/culvers_fotd.star` has known drifted values. Update these to match canonical flavor-colors.js:
       - BASE_COLORS: `butter_pecan` should be `#F2E7D1` (was `#D4A574`)
       - Add missing BASE_COLORS entries: `chocolate_custard` -> `#5A3825`, `mint_andes` -> `#1A8A4A`, `lemon` -> `#FFF176`, `blackberry` -> `#6B3FA0`
       - RIBBON_COLORS: `caramel` should be `#D38B2C` (was `#DAA520`)
       - TOPPING_COLORS: `andes` should be `#1FAE7A` (was `#00897B`), `dove` should be `#2B1A12` (was `#3B1F0B`), `pecan` should be `#8B5A2B` (was `#8B6914`)
       - Add missing TOPPING_COLORS entries: `brownie` -> `#2D1700`, `blueberry` -> `#3B1F6B`, `pie_crust` -> `#C4966A`, `blackberry_drupe` -> `#3D1F5C`, `cheesecake_bits` -> `#FFF8DC`
       - Verify the custard-calendar/tidbyt/culvers_fotd.star copy is already in sync (Phase 13-01 fixed it). If any entries are missing there too, add them.

    NOTE: Use `import.meta.dirname` (Node 20+) for building absolute paths to files outside worker/ when reading sync targets. The Vitest config confirms Node.js environment (not Cloudflare pool).
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npx vitest run flavor-colors.test.js -x</automated>
  </verify>
  <done>
    - pixelmatch and pngjs appear in worker/package.json devDependencies
    - render-to-pixels.js exports renderToPixels, stableHash, pixelMapToRGBA
    - flavor-colors.test.js imports from helpers and all existing 810+ tests pass
    - custard-tidbyt Starlark colors match canonical for all BASE/RIBBON/TOPPING entries
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Palette sync test (VALD-01)</name>
  <files>custard-calendar/worker/test/palette-sync.test.js</files>
  <behavior>
    - Test: All BASE_COLORS keys+values in cone-renderer.js FALLBACK_BASE_COLORS match canonical
    - Test: All RIBBON_COLORS keys+values in cone-renderer.js FALLBACK_RIBBON_COLORS match canonical
    - Test: All TOPPING_COLORS keys+values in cone-renderer.js FALLBACK_TOPPING_COLORS match canonical
    - Test: All CONE_COLORS keys+values in cone-renderer.js FALLBACK_CONE_COLORS match canonical
    - Test: All BASE_COLORS in tidbyt/culvers_fotd.star match canonical
    - Test: All RIBBON_COLORS in tidbyt/culvers_fotd.star match canonical
    - Test: All TOPPING_COLORS in tidbyt/culvers_fotd.star match canonical
    - Test: Cone color hex values (#D2691E, #B8860B) appear in tidbyt/culvers_fotd.star
    - Test: Same checks for custard-tidbyt/apps/culversfotd/culvers_fotd.star (skip gracefully if path missing -- not in CI checkout)
    - Test: All SEED_BASE keys+values in flavor-audit.html match canonical BASE_COLORS
    - Test: All SEED_RIBBON in flavor-audit.html match canonical RIBBON_COLORS
    - Test: All SEED_TOPPING in flavor-audit.html match canonical TOPPING_COLORS
    - Test: SEED_CONE in flavor-audit.html matches canonical CONE_COLORS
  </behavior>
  <action>
    Create `worker/test/palette-sync.test.js` that verifies all 5 sync files match canonical flavor-colors.js.

    Structure:
    1. Import canonical color dicts from `../src/flavor-colors.js`
    2. Read each downstream file with `fs.readFileSync()` using paths resolved from `import.meta.dirname`
    3. Parse color values with regex:
       - cone-renderer.js: `var FALLBACK_{DICT_NAME} = { key: '#HEXVAL', ... }` pattern
       - Starlark: `DICT_NAME = {\n  "key": "#HEXVAL",\n  ... }` pattern (double quotes)
       - flavor-audit.html: `var SEED_{NAME} = { key:'#HEXVAL', ... }` pattern (single quotes, may also have double quotes)
    4. For each dict type (BASE, RIBBON, TOPPING, CONE), compare every canonical entry against parsed downstream values. Normalize all hex values to uppercase for comparison.
    5. For Starlark CONE_COLORS: since there is no named dict, instead verify that both cone hex values (#D2691E and #B8860B) appear somewhere in the Starlark file content.
    6. For custard-tidbyt path: use `fs.existsSync()` to check `path.resolve(import.meta.dirname, '../../../../custard-tidbyt/apps/culversfotd/culvers_fotd.star')`. If missing, use `describe.skip()` with a console.warn explaining it is not available in CI. If present, run full sync checks.

    Test organization: one `describe` block per sync file, with individual `it()` per color dict type within each describe. Each test assertion should include the specific key and expected vs actual value in the error message for easy debugging.

    File paths relative to worker/test/:
    - canonical: `../src/flavor-colors.js` (ESM import)
    - cone-renderer: `../../docs/cone-renderer.js` (fs read)
    - tidbyt star: `../../tidbyt/culvers_fotd.star` (fs read)
    - custard-tidbyt star: `../../../../custard-tidbyt/apps/culversfotd/culvers_fotd.star` (fs read, may not exist)
    - flavor-audit: `../../docs/flavor-audit.html` (fs read)
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npx vitest run palette-sync.test.js -x</automated>
  </verify>
  <done>
    - palette-sync.test.js passes with zero failures
    - Every canonical color key is verified against all 4 downstream files (5 for custard-tidbyt if available)
    - Test would fail if any hex value is changed in a downstream file without updating canonical
    - CONE_COLORS checked via FALLBACK dict (cone-renderer.js), SEED_CONE (HTML), and inline hex presence (Starlark)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contrast checker (VALD-02)</name>
  <files>custard-calendar/worker/test/contrast-check.test.js</files>
  <behavior>
    - Test: Every FLAVOR_PROFILES entry has its topping colors checked against its base color
    - Test: All topping/base combinations meet >= 3:1 WCAG contrast ratio
    - Test: Known edge cases are explicitly tested (dove on dark_chocolate, fudge ribbon on dark_chocolate)
    - Test: relativeLuminance and contrastRatio utility functions produce correct values for known inputs
  </behavior>
  <action>
    Create `worker/test/contrast-check.test.js` that enforces 3:1 minimum contrast for all topping/base combinations.

    Implementation:
    1. Import FLAVOR_PROFILES, BASE_COLORS, TOPPING_COLORS from `../src/flavor-colors.js`
    2. Implement `relativeLuminance(hex)` using WCAG 2.0 formula:
       - Parse R/G/B from hex string
       - Apply sRGB linearization: c <= 0.03928 ? c/12.92 : ((c+0.055)/1.055)^2.4
       - Return 0.2126*R + 0.7152*G + 0.0722*B
    3. Implement `contrastRatio(hex1, hex2)`:
       - (lighter + 0.05) / (darker + 0.05)
    4. Test structure:
       - `describe('WCAG contrast utilities')`: unit tests for relativeLuminance (black=0, white=1) and contrastRatio (black/white=21:1)
       - `describe('topping/base contrast >= 3:1')`: iterate all FLAVOR_PROFILES entries. For each profile, resolve base color from BASE_COLORS[profile.base]. For each unique topping in profile.toppings, resolve color from TOPPING_COLORS[topping]. Assert contrastRatio >= 3.0. Use descriptive test name: `it('flavorName: toppings[i] on profile.base')`.
    5. If any existing profiles fail the 3:1 threshold, the test must report which specific combinations fail. Fix the offending color values in flavor-colors.js to meet 3:1 (adjust the topping or base color minimally -- prefer lightening a dark topping or darkening a light topping by the smallest amount needed). Document any color adjustments as comments.
    6. If a color adjustment is needed, also update all 4 downstream sync files to match (or note that palette-sync.test.js from Task 2 will catch the drift on next run).
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npx vitest run contrast-check.test.js -x</automated>
  </verify>
  <done>
    - contrast-check.test.js passes with zero failures
    - Every profiled flavor's topping/base combinations verified at >= 3:1 ratio
    - Any pre-existing contrast failures have been fixed (colors adjusted in canonical + downstream files)
    - WCAG utility functions tested with known reference values
  </done>
</task>

</tasks>

<verification>
Run full worker test suite to confirm no regressions:
```bash
cd custard-calendar/worker && npm test
```

All tests pass including:
- Existing 810+ flavor-colors tests (using extracted shared utility)
- New palette-sync tests (VALD-01)
- New contrast-check tests (VALD-02)
</verification>

<success_criteria>
- `cd custard-calendar/worker && npx vitest run palette-sync.test.js` passes (VALD-01)
- `cd custard-calendar/worker && npx vitest run contrast-check.test.js` passes (VALD-02)
- `cd custard-calendar/worker && npm test` passes (no regressions)
- Shared test utility exists at worker/test/helpers/render-to-pixels.js
- custard-tidbyt Starlark colors match canonical (no drift)
</success_criteria>

<output>
After completion, create `.planning/phases/14-validation-tooling/14-01-SUMMARY.md`
</output>
