---
phase: 16-bulk-profile-authoring
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - custard-calendar/worker/src/flavor-colors.js
  - custard-calendar/docs/flavor-audit.html
  - custard-calendar/docs/cone-renderer.js
  - custard-calendar/worker/test/fixtures/goldens/mini/*.png
  - custard-calendar/worker/test/fixtures/goldens/hd/*.png
  - custard-calendar/worker/test/fixtures/goldens/premium/*.png
  - custard-calendar/worker/test/fixtures/goldens/hero/*.png
autonomous: false
requirements: [PROF-03]

must_haves:
  truths:
    - "All fruit-family and specialty-family flavors have FLAVOR_PROFILES entries that pass contrast checker"
    - "Golden baselines exist for every new fruit/specialty profile"
    - "Zero unprofiled or keyword-fallback entries remain in flavor-audit.html for any known flavor"
    - "All ~107+ previously unprofiled flavors now have profiles or aliases"
  artifacts:
    - path: "custard-calendar/worker/src/flavor-colors.js"
      provides: "Complete FLAVOR_PROFILES covering all known flavors"
      contains: "FLAVOR_PROFILES"
    - path: "custard-calendar/docs/flavor-audit.html"
      provides: "SEED_PROFILES + SEED_CATALOG matching canonical for all flavors"
      contains: "SEED_PROFILES"
  key_links:
    - from: "custard-calendar/worker/src/flavor-colors.js"
      to: "custard-calendar/docs/flavor-audit.html"
      via: "manual sync of FLAVOR_PROFILES -> SEED_PROFILES"
      pattern: "SEED_PROFILES.*base.*ribbon.*toppings.*density"
    - from: "custard-calendar/worker/src/flavor-colors.js"
      to: "custard-calendar/worker/test/fixtures/goldens"
      via: "UPDATE_GOLDENS=1 npx vitest run golden-baselines"
      pattern: "golden.*png"
---

<objective>
Author FLAVOR_PROFILES entries for all remaining unprofiled flavors (fruit-family, specialty-family, and any stragglers) and verify zero unprofiled flavors remain.

Purpose: Complete bulk profile authoring. After this plan, every known flavor in the catalog has a proper profile or alias -- the PROF-03 requirement is fully satisfied.

Output: Fruit-family and specialty-family FLAVOR_PROFILES entries in all sync files, golden baselines, and human verification that flavor-audit.html shows zero unprofiled entries.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-bulk-profile-authoring/16-02-SUMMARY.md
@.planning/phases/16-bulk-profile-authoring/16-UNPROFILED.md

<interfaces>
<!-- Profile structure and sync pattern from prior plans. -->

Profile entry format (flavor-colors.js):
'flavor name lowercase': {
  base: 'base_color_key',
  ribbon: 'ribbon_color_key' | null,
  toppings: ['topping_key1', ...],
  density: 'pure'|'standard'|'double'|'explosion'|'overload'
}

Fruit-family bases: strawberry, peach, cherry, blackberry, lemon, orange, banana, raspberry, blueberry (note: some of these are toppings not bases -- use the base that matches the custard)
Specialty-family bases: mint, mint_andes, cheesecake, pumpkin, root_beer, pistachio, coconut, blue_moon

Files to update per batch (ALL atomically):
1. worker/src/flavor-colors.js: FLAVOR_PROFILES + FLAVOR_ALIASES (canonical)
2. docs/flavor-audit.html: SEED_PROFILES + SEED_ALIASES + SEED_CATALOG
3. docs/cone-renderer.js: FALLBACK_FLAVOR_ALIASES (only if new aliases)
4. worker/test/fixtures/goldens/**/*.png: 4 PNGs per new flavor

Density guide:
- pure: no mix-ins
- standard: 1-3 mix-in types
- double: "Double" in name or heavy single-ingredient
- explosion: 3+ distinct mix-in types
- overload: "Overload" in name
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author fruit-family and specialty-family FLAVOR_PROFILES with sync and golden baselines</name>
  <files>custard-calendar/worker/src/flavor-colors.js, custard-calendar/docs/flavor-audit.html, custard-calendar/docs/cone-renderer.js, custard-calendar/worker/test/fixtures/goldens</files>
  <action>
Read 16-UNPROFILED.md for the remaining unprofiled flavors: fruit-family (strawberry, peach, cherry, blackberry, lemon, orange, banana, raspberry bases) and specialty-family (mint, cheesecake, pumpkin, root_beer, pistachio, coconut, blue_moon bases). Also pick up any flavors that were not categorized in prior families.

For each unprofiled flavor:
1. Determine base from name/description. Fruit custards use the fruit as the base (e.g., cherry base for Cherry Cooler). Specialty custards use their defining flavor (e.g., pumpkin base for Pumpkin Pecan).
2. Determine ribbon, toppings, and density using established methodology.
3. Reference Culver's website images where available.

Add entries to FLAVOR_PROFILES in `worker/src/flavor-colors.js` (canonical, alphabetical insertion).

Add aliases for name variants to FLAVOR_ALIASES.

Sync atomically to all files:
- `worker/src/flavor-colors.js`: FLAVOR_PROFILES + FLAVOR_ALIASES
- `docs/flavor-audit.html`: SEED_PROFILES + SEED_ALIASES + SEED_CATALOG
- `docs/cone-renderer.js`: FALLBACK_FLAVOR_ALIASES (only if new aliases)

Run CI gates:
1. `cd custard-calendar/worker && npx vitest run contrast-check.test.js -x`
2. `cd custard-calendar/worker && npx vitest run alias-validation.test.js -x`
3. `cd custard-calendar/worker && UPDATE_GOLDENS=1 npx vitest run golden-baselines.test.js`
4. `cd custard-calendar/worker && npx vitest run golden-baselines.test.js -x`
5. `cd custard-calendar/worker && npx vitest run palette-sync.test.js -x`

After all profiles are added, do a final sweep:
- Cross-reference 16-UNPROFILED.md: every listed flavor should now have either a FLAVOR_PROFILES entry or a FLAVOR_ALIASES mapping.
- Run `cd custard-calendar/worker && npm test` to verify the full test suite passes.
- Count total FLAVOR_PROFILES entries and verify it equals (original 40 + total new profiles from Plans 01, 02, and 03).
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npm test</automated>
  </verify>
  <done>All fruit-family and specialty-family unprofiled flavors have FLAVOR_PROFILES entries. Every flavor from 16-UNPROFILED.md is either profiled or aliased. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual review of all profiled flavors in flavor-audit.html</name>
  <files>custard-calendar/docs/flavor-audit.html</files>
  <action>
Human reviews rendered cone previews for all profiled flavors in flavor-audit.html to verify visual accuracy.

What was built: Complete bulk profile authoring -- all previously unprofiled flavors now have FLAVOR_PROFILES entries across all sync files with golden baselines. The full CI suite passes.

How to verify:
1. Open `custard-calendar/docs/flavor-audit.html` in a browser (works offline with SEED_ data).
2. Check the stats bar at the top -- "profiles" count should be 100+ (was 40 before Phase 16).
3. Scroll through the flavor grid. Look for:
   - NO "no profile" badges on any flavor card
   - NO "keyword fallback" badges on any flavor card
   - Every cone rendering looks visually reasonable (correct base color, visible toppings)
4. Spot-check 5-10 specific flavors:
   - Does "Cappuccino Cookie Crumble" look espresso-brown?
   - Does any pumpkin flavor look orange-brown?
   - Do cherry flavors look red?
   - Do mint flavors look green?
5. Verify the alias count increased (was 20, should be higher).
6. If any flavors look wrong or are missing profiles, describe the issues.

Resume signal: Type "approved" if all flavors look correct, or describe specific issues.
  </action>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated command"</automated>
  </verify>
  <done>User has reviewed flavor-audit.html and approved that all rendered cones look visually accurate with no "no profile" or "keyword fallback" badges remaining.</done>
</task>

</tasks>

<verification>
- `cd custard-calendar/worker && npm test` -- full test suite passes
- All flavors from 16-UNPROFILED.md have profiles or aliases
- flavor-audit.html shows zero "no profile" or "keyword fallback" entries
- Human verification: rendered cones look visually accurate
</verification>

<success_criteria>
- Zero unprofiled flavors remain in the catalog
- All CI gates pass (contrast, palette-sync, alias-validation, golden-baselines, full suite)
- flavor-audit.html visual review approved by user
- PROF-03 requirement fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-profile-authoring/16-03-SUMMARY.md`
</output>
