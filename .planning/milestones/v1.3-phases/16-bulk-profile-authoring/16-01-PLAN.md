---
phase: 16-bulk-profile-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custard-calendar/worker/src/flavor-colors.js
  - custard-calendar/docs/flavor-audit.html
  - custard-calendar/docs/cone-renderer.js
  - custard-calendar/worker/test/fixtures/goldens/mini/*.png
  - custard-calendar/worker/test/fixtures/goldens/hd/*.png
  - custard-calendar/worker/test/fixtures/goldens/premium/*.png
  - custard-calendar/worker/test/fixtures/goldens/hero/*.png
autonomous: true
requirements: [PROF-03]

must_haves:
  truths:
    - "All unprofiled flavors are identified and categorized by family"
    - "Chocolate-family flavors have FLAVOR_PROFILES entries that pass contrast checker"
    - "Golden baselines exist for every new chocolate-family profile"
  artifacts:
    - path: "custard-calendar/worker/src/flavor-colors.js"
      provides: "Canonical FLAVOR_PROFILES with new chocolate-family entries"
      contains: "FLAVOR_PROFILES"
    - path: "custard-calendar/docs/flavor-audit.html"
      provides: "SEED_PROFILES + SEED_CATALOG matching canonical for chocolate-family batch"
      contains: "SEED_PROFILES"
  key_links:
    - from: "custard-calendar/worker/src/flavor-colors.js"
      to: "custard-calendar/docs/flavor-audit.html"
      via: "manual sync of FLAVOR_PROFILES -> SEED_PROFILES"
      pattern: "SEED_PROFILES.*base.*ribbon.*toppings.*density"
    - from: "custard-calendar/worker/src/flavor-colors.js"
      to: "custard-calendar/worker/test/fixtures/goldens"
      via: "UPDATE_GOLDENS=1 npx vitest run golden-baselines"
      pattern: "golden.*png"
---

<objective>
Discover all unprofiled flavors in the catalog and author FLAVOR_PROFILES entries for the chocolate-family batch.

Purpose: Establish the complete unprofiled flavor list (blocking subsequent plans) and deliver the first batch of profiles for chocolate-based, dark-chocolate-based, and espresso-based flavors.

Output: Categorized unprofiled flavor list (written to a scratch file for subsequent plans), chocolate-family FLAVOR_PROFILES entries in all sync files, golden baselines for new profiles.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-palette-expansion-aliases/15-01-SUMMARY.md
@.planning/phases/15-palette-expansion-aliases/15-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts from the codebase. -->

From custard-calendar/worker/src/flavor-colors.js:
- FLAVOR_PROFILES: object keyed by normalizeFlavorKey() output (lowercase, no TM/R, curly quotes normalized)
- Each profile: { base: string, ribbon: string|null, toppings: string[], density: 'pure'|'standard'|'double'|'explosion'|'overload' }
- FLAVOR_ALIASES: object mapping normalized variant name -> canonical profile key
- BASE_COLORS: 23 keys (vanilla, chocolate, chocolate_custard, dark_chocolate, mint, mint_andes, strawberry, cheesecake, caramel, butter_pecan, peach, lemon, blackberry, espresso, cherry, pumpkin, banana, coconut, root_beer, pistachio, orange, blue_moon, maple)
- RIBBON_COLORS: 5 keys (caramel, peanut_butter, marshmallow, chocolate_syrup, fudge)
- TOPPING_COLORS: 33 keys (oreo, andes, dove, pecan, cashew, heath, butterfinger, cookie_dough, strawberry_bits, raspberry, peach_bits, salt, snickers, cake, cheesecake_bits, m_and_m, reeses, brownie, blueberry, pie_crust, blackberry_drupe, chocolate_chip, sprinkles, graham_cracker, coconut_flakes, cherry_bits, caramel_chips, pretzel, pumpkin_spice, marshmallow_bits, candy_cane, cookie_crumbs, fudge_bits)

From custard-calendar/worker/src/flavor-catalog.js:
- SEED_CATALOG: 39 entries with { title, description } (static seed, all already profiled)
- getFlavorCatalog(kv): merges SEED_CATALOG with KV-accumulated flavors
- /api/flavors/catalog endpoint returns full catalog

From custard-calendar/docs/flavor-audit.html:
- SEED_PROFILES: var (not const), copy of FLAVOR_PROFILES
- SEED_ALIASES: var, copy of FLAVOR_ALIASES
- SEED_CATALOG: var array, copy of flavor catalog entries
- SEED_BASE, SEED_RIBBON, SEED_TOPPING, SEED_CONE: color dicts
- API fetch overlays live data on top of SEED_ fallbacks

From custard-calendar/docs/cone-renderer.js:
- FALLBACK_FLAVOR_ALIASES: copy of FLAVOR_ALIASES (update only when new aliases added)
- NO FALLBACK_FLAVOR_PROFILES exists -- profiles come from API response
- FALLBACK_BASE_COLORS, FALLBACK_RIBBON_COLORS, FALLBACK_TOPPING_COLORS: color dicts (no changes needed)

Density guide:
- pure: no mix-ins (e.g., Vanilla, Dark Chocolate Decadence)
- standard: 1-3 mix-in types
- double: "Double" in name, or heavy single-ingredient focus
- explosion: 3+ distinct mix-in types, very loaded
- overload: "Overload" in name, massive single-topping coverage
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover all unprofiled flavors and categorize by family</name>
  <files>custard-calendar/worker/src/flavor-colors.js, custard-calendar/worker/src/flavor-catalog.js, .planning/phases/16-bulk-profile-authoring/16-UNPROFILED.md</files>
  <action>
Discover the full set of unprofiled flavors by combining all known flavor sources:

1. Read the SEED_CATALOG from `worker/src/flavor-catalog.js` (39 entries).
2. Read KNOWN_FLAVORS_FALLBACK from `docs/map.html` (32 entries).
3. Read all flavors from SIMILARITY_GROUPS in `worker/src/flavor-matcher.js`.
4. Read the FLAVOR_FAMILIES object in `worker/src/flavor-matcher.js` for family classification hints.
5. Read any additional flavor names from `docs/group.html` (KNOWN_FLAVORS_FALLBACK there too).
6. Fetch the live catalog from the production API: `curl -s https://custard.chriskaschner.com/api/flavors/catalog | node -e "process.stdin.on('data',d=>{const j=JSON.parse(d);j.flavors.forEach(f=>console.log(f.title))})"` to get KV-accumulated flavors.
7. Also check the Culver's FOTD page at https://www.culvers.com/flavor-of-the-day for any flavors listed there.

Combine all sources into a deduplicated set. For each flavor, check if it already has:
- A FLAVOR_PROFILES entry (40 existing)
- A FLAVOR_ALIASES entry mapping to a profiled key (20 existing)
- A keyword fallback match (via getFlavorProfile logic)

Any flavor that has NONE of these is "unprofiled" and needs a new profile or alias.

Write the categorized list to `.planning/phases/16-bulk-profile-authoring/16-UNPROFILED.md` with:
- Total unprofiled count
- Flavors grouped by family:
  - **Chocolate family**: chocolate, dark_chocolate, chocolate_custard, espresso bases
  - **Vanilla family**: vanilla, butter_pecan bases
  - **Caramel family**: caramel, maple bases
  - **Fruit family**: strawberry, peach, cherry, blackberry, lemon, orange, banana, raspberry, blueberry bases
  - **Specialty family**: mint, mint_andes, cheesecake, pumpkin, root_beer, pistachio, coconut, blue_moon bases
- For each flavor: suggested base, description (from catalog), and which source it came from

For Culver's-specific flavors, reference https://www.culvers.com/flavor-of-the-day to visually confirm custard colors. For non-Culver's brands (Kopp's, Gille's, etc.), use description-based color inference.

Do NOT create any FLAVOR_PROFILES entries yet -- this task is discovery only.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-bulk-profile-authoring/16-UNPROFILED.md && grep -c "^-" .planning/phases/16-bulk-profile-authoring/16-UNPROFILED.md</automated>
  </verify>
  <done>16-UNPROFILED.md exists with a categorized list of all unprofiled flavors, grouped by family, with suggested bases and descriptions. The count is definitive -- every known flavor is either profiled, aliased, or listed as unprofiled.</done>
</task>

<task type="auto">
  <name>Task 2: Author chocolate-family FLAVOR_PROFILES with sync and golden baselines</name>
  <files>custard-calendar/worker/src/flavor-colors.js, custard-calendar/docs/flavor-audit.html, custard-calendar/docs/cone-renderer.js, custard-calendar/worker/test/fixtures/goldens</files>
  <action>
Using the chocolate-family list from 16-UNPROFILED.md, author FLAVOR_PROFILES entries for all chocolate-based, dark-chocolate-based, chocolate-custard-based, and espresso-based unprofiled flavors.

For each flavor:
1. Determine base color from the name/description: chocolate, dark_chocolate, chocolate_custard, or espresso.
2. Determine ribbon: caramel (for caramel swirls), peanut_butter (for PB), marshmallow (for marshmallow swirl), chocolate_syrup (for fudge swirl), fudge, or null.
3. Determine toppings from description keywords: map ingredient words to existing TOPPING_COLORS keys. Use ONLY existing topping keys (33 available). If a topping has no matching key, use the closest equivalent.
4. Determine density: pure (no mix-ins), standard (1-3 types), double ("double" in name), explosion (3+ types), overload ("overload" in name).
5. Reference Culver's website flavor images where available for color accuracy.

Add entries to FLAVOR_PROFILES in `worker/src/flavor-colors.js` (canonical). Alphabetical insertion within existing entries.

If any new flavor name variants are discovered that should alias to a canonical profile, add them to FLAVOR_ALIASES.

Sync updates atomically:
- `worker/src/flavor-colors.js`: Add to FLAVOR_PROFILES + FLAVOR_ALIASES
- `docs/flavor-audit.html`: Add to SEED_PROFILES (same structure, var not const) + SEED_ALIASES (if new aliases) + SEED_CATALOG (new { title, description } entries)
- `docs/cone-renderer.js`: Add to FALLBACK_FLAVOR_ALIASES only (if new aliases added)

After all entries are added, run:
1. `cd custard-calendar/worker && npx vitest run contrast-check.test.js -x` -- if any failures, adjust topping color choice or add contrast exemption per Phase 14 pattern (prefer adjusting topping choice first)
2. `cd custard-calendar/worker && npx vitest run alias-validation.test.js -x` -- verify alias integrity
3. `cd custard-calendar/worker && UPDATE_GOLDENS=1 npx vitest run golden-baselines.test.js` -- generate new golden PNGs (4 tiers per new flavor)
4. `cd custard-calendar/worker && npx vitest run golden-baselines.test.js -x` -- verify goldens match
5. `cd custard-calendar/worker && npx vitest run palette-sync.test.js -x` -- verify color sync (colors only, not profiles)
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npx vitest run contrast-check.test.js palette-sync.test.js alias-validation.test.js golden-baselines.test.js -x</automated>
  </verify>
  <done>All chocolate-family unprofiled flavors have FLAVOR_PROFILES entries. All 4 sync files updated. Contrast checker passes. Golden baselines generated for all new profiles. 16-UNPROFILED.md chocolate-family section can be checked off.</done>
</task>

</tasks>

<verification>
- `cd custard-calendar/worker && npx vitest run contrast-check.test.js palette-sync.test.js alias-validation.test.js golden-baselines.test.js -x` -- all tests pass
- FLAVOR_PROFILES count increased by chocolate-family count
- 16-UNPROFILED.md exists with complete categorized list
- SEED_PROFILES in flavor-audit.html matches new profiles in flavor-colors.js
</verification>

<success_criteria>
- 16-UNPROFILED.md contains definitive categorized list of all unprofiled flavors
- All chocolate/dark-chocolate/espresso-family flavors have profiles
- All CI gates pass (contrast, palette-sync, alias-validation, golden-baselines)
- SEED_PROFILES and SEED_CATALOG in flavor-audit.html updated
- FALLBACK_FLAVOR_ALIASES in cone-renderer.js updated (if new aliases)
</success_criteria>

<output>
After completion, create `.planning/phases/16-bulk-profile-authoring/16-01-SUMMARY.md`
</output>
