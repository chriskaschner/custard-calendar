---
phase: 30-housekeeping-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [TODO.md]
autonomous: true
requirements: [SIMP-03]

must_haves:
  truths:
    - "ML prediction pipeline items (ensemble, XGBoost, confidence intervals, cluster transfer) appear in a Won't Do section with documented rationale"
    - "Every unchecked TODO item from prior milestones is either checked off with rationale, promoted to v3.0 scope, or explicitly deferred to Someday/Maybe or Won't Do"
    - "The TODO.md backlog reflects reality -- no stale open items from completed or abandoned work streams"
  artifacts:
    - path: "TODO.md"
      provides: "Triaged backlog with Won't Do section and all stale items resolved"
      contains: "## Won't Do"
  key_links:
    - from: "TODO.md"
      to: ".planning/REQUIREMENTS.md"
      via: "SIMP-03 requirement satisfaction"
      pattern: "Won't Do"
---

<objective>
Triage every open item in TODO.md: move ML prediction pipeline items to a new "Won't Do" section with documented rationale, and resolve all other stale/unchecked items from prior milestones by closing, promoting, or deferring them.

Purpose: The project backlog has accumulated unchecked items across completed milestones. Many are stale (superseded by v2.0 art pipeline, already addressed by shipped features, or explicitly deprioritized by strategic decisions). Cleaning this up makes the backlog trustworthy for v3.0 planning.

Output: A triaged TODO.md where every open item has a clear disposition.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move ML prediction items to Won't Do with rationale</name>
  <files>TODO.md</files>
  <read_first>
    - TODO.md (current state of all items)
    - .planning/REQUIREMENTS.md (SIMP-03 definition: "ML prediction roadmap items formally closed")
    - .planning/ROADMAP.md (Phase 30 success criteria)
  </read_first>
  <action>
Create a new `## Won't Do` section in TODO.md between `## Someday/Maybe` and `## Data Health`. Move these four items from Someday/Maybe into it, changing `[ ]` to `[x]` and appending rationale:

1. **Ensemble predictor** -- move from Someday/Maybe line 395. Mark as:
   `- [x] **Ensemble predictor** -- Won't Do. FR/Markov/PCA ensemble targets ~5-6% top-1 accuracy from 3.2% baseline. The confirmed schedule IS the product (99%+ accuracy); prediction accuracy improvements have no user-visible value. Closed 2026-03-19.`

2. **Confidence intervals in forecast output** -- move from Someday/Maybe line 396. Mark as:
   `- [x] **Confidence intervals in forecast output** -- Won't Do. P95 uncertainty bands on predictions are meaningless when predictions themselves have 3.2% accuracy. The product surfaces confirmed schedules, not probabilistic forecasts. Closed 2026-03-19.`

3. **Cluster-based transfer learning** -- move from Someday/Maybe line 397. Mark as:
   `- [x] **Cluster-based transfer learning** -- Won't Do. PCA cluster centroids as priors for sparse stores is prediction infrastructure with no surface value. Confirmed schedule data from upstream sites provides 100% accuracy for covered stores. Closed 2026-03-19.`

4. **Cluster-personalized forecast emails** -- move from Someday/Maybe line 398. Mark as:
   `- [x] **Cluster-personalized forecast emails** -- Won't Do. Comparing store to PCA cluster centroid for email personalization is prediction-framed. Weekly digest emails already ship confirmed schedules and flavor signals -- no user value from adding probabilistic cluster comparisons. Closed 2026-03-19.`

The Won't Do section should have a header note:
```
## Won't Do

Formally closed items. Documented rationale for why these will not be built.

### ML Prediction Pipeline (closed 2026-03-19)

Strategic rationale: The analytics pipeline confirmed that Culver's publishes flavor schedules deterministically. Confirmed schedule accuracy is ~100%; ML prediction top-1 accuracy is 3.2%. The confirmed schedule IS the product. Prediction infrastructure has no user-visible value and would add maintenance burden without improving the core experience. See REQUIREMENTS.md SIMP-03.
```
  </action>
  <verify>
    <automated>grep -c "Won't Do" TODO.md && grep "Ensemble predictor" TODO.md | grep -c "Won't Do" && grep "Confidence intervals" TODO.md | grep -c "Won't Do" && grep "Cluster-based transfer" TODO.md | grep -c "Won't Do" && grep "Cluster-personalized" TODO.md | grep -c "Won't Do"</automated>
  </verify>
  <acceptance_criteria>
    - TODO.md contains a `## Won't Do` section header
    - TODO.md contains `### ML Prediction Pipeline (closed 2026-03-19)` subsection
    - The string "Ensemble predictor" appears in the Won't Do section with `[x]` marker
    - The string "Confidence intervals in forecast output" appears in the Won't Do section with `[x]` marker
    - The string "Cluster-based transfer learning" appears in the Won't Do section with `[x]` marker
    - The string "Cluster-personalized forecast emails" appears in the Won't Do section with `[x]` marker
    - All four items are removed from the Someday/Maybe section (no duplicates)
    - Each closed item includes a rationale sentence explaining why it will not be built
    - The section includes the strategic rationale paragraph referencing SIMP-03
  </acceptance_criteria>
  <done>All four ML prediction pipeline items appear in a Won't Do section with individual rationale and a strategic summary. Zero ML prediction items remain in Someday/Maybe.</done>
</task>

<task type="auto">
  <name>Task 2: Triage all remaining stale TODO items</name>
  <files>TODO.md</files>
  <read_first>
    - TODO.md (after Task 1 modifications)
    - .planning/ROADMAP.md (v3.0 phases 30-34 to know what is already scoped)
  </read_first>
  <action>
Resolve every remaining unchecked `[ ]` item in TODO.md that is NOT already tracked in v3.0 ROADMAP phases 31-34. For each item, apply one of three dispositions:

**A. Close with rationale** (mark `[x]`, append "-- Closed [date]. [reason]"):

1. `[ ] Route profile roadmap` (Phase 2 preference model, line 116) -- Close: "Closed 2026-03-19. Named multi-route profiles deferred indefinitely. Single active route covers the primary use case; multi-route adds complexity with no validated user demand. Tracked as ROUTE-01 in REQUIREMENTS.md Future Requirements if revisited."

2. `[ ] Cone icon sizing in Drive cards` (P0 homepage audit, line 125) -- Close: "Closed 2026-03-19. Superseded by v2.0 art pipeline -- Drive cards now use L5 AI PNG cones, not renderMiniConeSVG. Homepage redesign (Phase 31) will establish the final card layout."

3. `[ ] Drive card vs hero card inconsistency` (line 126) -- Close: "Closed 2026-03-19. Phase 31 homepage redesign will rebuild the hero card from scratch, making this inconsistency moot."

4. `[ ] Minimap mostly blank` (line 127) -- Close: "Closed 2026-03-19. Minimap is a secondary element. Homepage redesign (Phase 31) may remove or redesign the minimap entirely. Not worth fixing independently."

5. `[ ] Score badge styling` (line 128) -- Close: "Closed 2026-03-19. Score badges will be reconsidered during homepage redesign (Phase 31). Current numeric scores may be removed entirely in favor of simpler trust signals."

6. `[ ] Week Ahead row cones` (line 129) -- Close: "Closed 2026-03-19. Superseded by L5 AI PNG art pipeline (v2.0). Week-ahead section redesign is scoped in Phase 31."

7. `[ ] Overall card/section hierarchy` (line 130) -- Close: "Closed 2026-03-19. Directly addressed by Phase 31 (homepage redesign) and Phase 32 (page consolidation) in v3.0 roadmap."

8. `[ ] Rarity/overdue copy fails sniff test` (line 131) -- Close: "Closed 2026-03-19. The seasonal cadence copy issue is a known data quality problem. Flavor hierarchy fallback (shipped in Data Health section) partially addresses this. Further refinement is out of scope for v3.0 -- the homepage redesign will determine which copy surfaces at all."

9. `[ ] Anonymous sync design spike` (line 137) -- Close: "Closed 2026-03-19. Anonymous sync deferred indefinitely. Tracked as SYNC-01 in REQUIREMENTS.md Future Requirements. No validated user demand; find 10 real users first."

10. `[ ] Persistence decision record` (line 138) -- Close: "Closed 2026-03-19. Dependent on sync design spike which is deferred. No standalone value."

11. `[ ] Security/privacy review for sync` (line 139) -- Close: "Closed 2026-03-19. Dependent on sync design spike which is deferred."

12. `[ ] Alerts placeholder copy review` (line 161) -- Close: "Closed 2026-03-19. The 'you@example.com' placeholder is fine as intentional UX copy. Not worth a separate work item."

13. `[ ] Flavor asset parity audit + canonicalization` (line 280) -- Close: "Closed 2026-03-19. v2.0 AI cone pipeline established a single canonical art source (L5 PNGs). Phases 27-29 unified all surfaces to this pipeline. Remaining parity is maintained by the shared art system."

14. `[ ] Canonical render spec` (line 281, the unchecked parent -- the sub-item is already checked) -- Close: "Closed 2026-03-19. v2.0 established L5 AI PNGs as the canonical render output. The hero cone renderer v2 sub-task already shipped. Multi-surface adapter architecture is unnecessary now that all surfaces consume the same PNG pipeline."

15. `[ ] Greenfield target architecture` (line 283) -- Close: "Closed 2026-03-19. Three-layer model documented in ARCHITECTURE.md. Incremental migration happens organically as features are built. No value in a standalone architecture migration effort."

16. `[ ] Greenfield target architecture: formalize layer discipline` (line 312) -- Close: "Closed 2026-03-19. ARCHITECTURE.md already documents the three-layer model with current gaps. Layer discipline is enforced incrementally, not as a standalone task."

**B. Keep in Someday/Maybe** (no change needed, already correctly placed):

These items are appropriately in Someday/Maybe and do not need triage action:
- `[ ] Go or add a topping? preference-driven decision card` -- creative future feature, correctly deferred
- `[ ] Flavor modification recommendations` -- dependent on above, correctly deferred
- `[ ] Flavor catalog browser with profile filtering` -- nice-to-have, correctly deferred
- `[ ] Alexa skill` -- platform expansion, correctly deferred (per REQUIREMENTS.md Out of Scope)
- `[ ] Android widget` -- platform expansion, correctly deferred (per REQUIREMENTS.md Out of Scope)
- `[ ] Madison-area brand expansion` -- scope expansion, correctly deferred
- `[ ] Flavor chatbot` -- scope expansion, correctly deferred
- `[ ] Tidbyt cone animation` -- polish, correctly deferred

**C. Keep open as active work** (part of v3.0 or ongoing):

- `[ ] Mad Libs quality + variety` (line 279) -- Move to Someday/Maybe. Append: "Moved from Refactor section 2026-03-19. Not critical path; revisit after v3.0 simplification."

Also: Mark the entire `## P0 -- Homepage visual coherence audit` section header with a note: "Note: Items below closed individually. Section superseded by v3.0 Phase 31 homepage redesign."

Mark the entire `## Phase 3 -- Optional sync architecture (P2+)` section header with a note: "Note: All items closed. Sync deferred indefinitely per v3.0 strategy (find users before adding features)."
  </action>
  <verify>
    <automated>grep -c "\- \[ \]" TODO.md</automated>
  </verify>
  <acceptance_criteria>
    - The count of unchecked `- [ ]` items in TODO.md outside of `## Someday/Maybe` is zero (all stale items in Now/Next/Later sections are resolved)
    - `Route profile roadmap` line contains `[x]` and "Closed 2026-03-19"
    - `Anonymous sync design spike` line contains `[x]` and "Closed 2026-03-19"
    - `Cone icon sizing in Drive cards` line contains `[x]` and "Closed 2026-03-19"
    - `Greenfield target architecture` lines contain `[x]` and "Closed 2026-03-19"
    - `Mad Libs quality + variety` appears in the Someday/Maybe section
    - The P0 Homepage visual coherence audit section has a superseded note
    - The Phase 3 Optional sync section has a closed note
    - All Someday/Maybe items that were correctly placed remain unchanged (Go or add a topping, Flavor modification, Flavor catalog, Alexa, Android, Madison expansion, Flavor chatbot, Tidbyt animation)
    - No items were accidentally deleted -- only dispositioned (closed, moved, or annotated)
  </acceptance_criteria>
  <done>Every unchecked item outside Someday/Maybe is resolved. The only remaining `[ ]` items in TODO.md are in Someday/Maybe (correctly deferred features) and Won't Do section has all ML items. The backlog accurately reflects what is active (v3.0 roadmap), what is deferred (Someday/Maybe), and what is permanently closed (Won't Do).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "## Won't Do" TODO.md` returns 1 (section exists)
2. `grep "Ensemble predictor\|Confidence intervals\|Cluster-based transfer\|Cluster-personalized" TODO.md | grep -c "\[x\]"` returns 4 (all ML items closed)
3. All `[ ]` items outside Someday/Maybe are zero: `awk '/## Someday/,0{next} /- \[ \]/' TODO.md | wc -l` returns 0
4. `git diff --stat TODO.md` shows the file was modified
</verification>

<success_criteria>
- TODO.md has a Won't Do section with 4 ML prediction pipeline items, each with individual rationale
- Zero stale unchecked items remain in completed milestone sections (Now, Next, Later sections)
- Someday/Maybe contains only intentionally deferred features (no stale items hiding there)
- The backlog is an accurate reflection of project reality as of v3.0 start
- SIMP-03 requirement is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/30-housekeeping-closure/30-01-SUMMARY.md`
</output>
