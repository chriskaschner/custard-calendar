---
phase: 10-redirects-css-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custard-calendar/tests/test_redirects.py
  - custard-calendar/tests/test_sw_precache.py
  - custard-calendar/tests/test_sw_registration.py
  - custard-calendar/docs/scoop.html
  - custard-calendar/docs/radar.html
  - custard-calendar/docs/calendar.html
  - custard-calendar/docs/widget.html
  - custard-calendar/docs/siri.html
  - custard-calendar/docs/alerts.html
  - custard-calendar/docs/multi.html
  - custard-calendar/docs/sw.js
  - custard-calendar/worker/test/browser/nav-clickthrough.spec.mjs
  - custard-calendar/worker/test/browser/scoop-compat.spec.mjs
  - custard-calendar/worker/test/browser/primary-store-persistence.spec.mjs
  - custard-calendar/worker/test/browser/shared-nav-store.spec.mjs
  - custard-calendar/worker/test/browser/drive-preferences.spec.mjs
  - custard-calendar/worker/test/browser/alerts-telemetry.spec.mjs
  - custard-calendar/worker/test/browser/radar-phase2.spec.mjs
  - custard-calendar/worker/test/browser/index-drive-defaults.spec.mjs
autonomous: true
requirements:
  - RDIR-01
  - RDIR-02

must_haves:
  truths:
    - "Visiting /scoop.html redirects to /index.html"
    - "Visiting /radar.html redirects to /index.html"
    - "Visiting /calendar.html redirects to /updates.html"
    - "Visiting /widget.html redirects to /updates.html"
    - "Visiting /siri.html redirects to /updates.html"
    - "Visiting /alerts.html redirects to /updates.html"
    - "Visiting /scoop.html?store=1234 preserves query params on redirect to index.html?store=1234"
    - "Visiting /alerts.html#section preserves hash fragment on redirect"
    - "Redirect stubs are minimal bare HTML with no shared-nav.js, style.css, or planner-shared.js loaded"
    - "multi.html redirects directly to index.html (no double-hop through scoop.html)"
    - "SW no longer caches calendar.html or widget.html stubs"
  artifacts:
    - path: "custard-calendar/tests/test_redirects.py"
      provides: "Static analysis tests for redirect stubs"
      min_lines: 50
    - path: "custard-calendar/docs/scoop.html"
      provides: "Redirect stub to index.html"
      contains: "meta http-equiv=\"refresh\""
    - path: "custard-calendar/docs/radar.html"
      provides: "Redirect stub to index.html"
      contains: "meta http-equiv=\"refresh\""
    - path: "custard-calendar/docs/calendar.html"
      provides: "Redirect stub to updates.html"
      contains: "meta http-equiv=\"refresh\""
    - path: "custard-calendar/docs/widget.html"
      provides: "Redirect stub to updates.html"
      contains: "meta http-equiv=\"refresh\""
    - path: "custard-calendar/docs/siri.html"
      provides: "Redirect stub to updates.html"
      contains: "meta http-equiv=\"refresh\""
    - path: "custard-calendar/docs/alerts.html"
      provides: "Redirect stub to updates.html"
      contains: "meta http-equiv=\"refresh\""
    - path: "custard-calendar/docs/sw.js"
      provides: "Updated STATIC_ASSETS and CACHE_VERSION"
      contains: "custard-v17"
  key_links:
    - from: "custard-calendar/docs/scoop.html"
      to: "index.html"
      via: "meta refresh + window.location.replace"
      pattern: "window\\.location\\.replace\\('index\\.html'"
    - from: "custard-calendar/docs/multi.html"
      to: "index.html"
      via: "meta refresh + window.location.replace (skip scoop.html hop)"
      pattern: "window\\.location\\.replace\\('index\\.html'"
    - from: "custard-calendar/docs/sw.js"
      to: "STATIC_ASSETS"
      via: "calendar.html and widget.html removed from array"
      pattern: "custard-v17"
---

<objective>
Replace 6 legacy HTML pages with minimal redirect stubs and update all dependent test infrastructure.

Purpose: Users with old bookmarks (scoop, radar, calendar, widget, siri, alerts) land on the correct new page with query params and hash fragments intact. Redirect stubs are bare HTML (~760 bytes each) -- no full JS stack loaded.
Output: 6 redirect stub files, updated multi.html, updated sw.js, new test_redirects.py, updated browser tests.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-redirects-css-cleanup/10-CONTEXT.md
@.planning/phases/10-redirects-css-cleanup/10-RESEARCH.md
@.planning/phases/10-redirects-css-cleanup/10-VALIDATION.md

<interfaces>
<!-- Redirect stub template (proven pattern from docs/multi.html): -->
```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="refresh" content="0;url={DESTINATION}">
  <title>Redirecting...</title>
</head>
<body>
  <p>Redirecting to <a href="{DESTINATION}">{DESTINATION}</a>...</p>
  <script>
    var q = window.location.search;
    var h = window.location.hash;
    window.location.replace('{DESTINATION}' + (q || '') + (h || ''));
  </script>
</body>
</html>
```

Redirect destination map:
- scoop.html -> index.html
- radar.html -> index.html
- calendar.html -> updates.html
- widget.html -> updates.html
- siri.html -> updates.html
- alerts.html -> updates.html

SW current state (docs/sw.js line 1-2):
- CACHE_VERSION = 'custard-v16'
- STATIC_ASSETS includes './calendar.html' (line 5) and './widget.html' (line 8)

multi.html currently redirects to scoop.html -- update to redirect directly to index.html.

Browser tests referencing affected pages (from RESEARCH.md pitfall list):
- nav-clickthrough.spec.mjs: visits /calendar.html, /scoop.html in ALL_PAGES list expecting #shared-nav
- scoop-compat.spec.mjs: visits /scoop.html?stores=... expecting Today's Drive content
- primary-store-persistence.spec.mjs: visits /alerts.html, /siri.html, /radar.html
- shared-nav-store.spec.mjs: visits /calendar.html
- drive-preferences.spec.mjs: visits /calendar.html
- alerts-telemetry.spec.mjs: visits /alerts.html
- radar-phase2.spec.mjs: visits /radar.html (4 test cases)
- index-drive-defaults.spec.mjs: visits /scoop.html?stores=...

Python tests:
- test_sw_registration.py: USER_FACING_PAGES includes widget.html, calendar.html; INLINE_PAGES includes widget.html, calendar.html
- test_sw_precache.py: TestCacheVersionBumped checks version != v15 (needs update to check != v16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create redirect test scaffold and replace 6 HTML pages with redirect stubs</name>
  <files>
    custard-calendar/tests/test_redirects.py,
    custard-calendar/docs/scoop.html,
    custard-calendar/docs/radar.html,
    custard-calendar/docs/calendar.html,
    custard-calendar/docs/widget.html,
    custard-calendar/docs/siri.html,
    custard-calendar/docs/alerts.html,
    custard-calendar/docs/multi.html,
    custard-calendar/docs/sw.js,
    custard-calendar/tests/test_sw_precache.py,
    custard-calendar/tests/test_sw_registration.py
  </files>
  <action>
    **Wave 0 -- Write tests first (TDD-lite):**

    1. Create `custard-calendar/tests/test_redirects.py` with these test classes:
       - `TestRedirectStubs`: For each of the 6 source files, read the HTML and verify it contains `<meta http-equiv="refresh" content="0;url={expected_destination}">`. Use a parameterized mapping dict: `{"scoop.html": "index.html", "radar.html": "index.html", "calendar.html": "updates.html", "widget.html": "updates.html", "siri.html": "updates.html", "alerts.html": "updates.html"}`.
       - `TestRedirectStubMinimal`: For each of the 6 files, verify the file does NOT contain `shared-nav.js`, `style.css`, `planner-shared.js`, or `today-page.js`. Also verify file size is under 1000 bytes.
       - `TestRedirectQueryParams`: For each of the 6 files, verify the HTML contains `window.location.search` (proving query param forwarding).
       - `TestRedirectHashFragments`: For each of the 6 files, verify the HTML contains `window.location.hash` (proving hash forwarding).
       - `TestMultiHtmlDirect`: Verify `multi.html` meta refresh points to `index.html` (not `scoop.html`).

       Use the same `DOCS_DIR = Path(__file__).resolve().parents[1] / "docs"` pattern as existing test files.

    2. Run tests -- they should FAIL (RED) since the 6 files are still full pages.

    **Wave 1 -- Create redirect stubs (GREEN):**

    3. Replace each of the 6 HTML files with the redirect stub template from the interfaces block above. Each file should be bare HTML only: `<!DOCTYPE html>`, `<html lang="en">`, `<head>` with `<meta charset="UTF-8">`, `<meta http-equiv="refresh" content="0;url={DESTINATION}">`, `<title>Redirecting...</title>`, `</head>`, `<body>` with a `<p>Redirecting to <a href="{DESTINATION}">{DESTINATION}</a>...</p>` and the JS fallback reading `window.location.search` and `window.location.hash`. No CSP meta tag needed (bare stubs).

    4. Update `docs/multi.html`: Change the meta refresh URL from `scoop.html` to `index.html`. Change the JS `window.location.replace('scoop.html'...)` to `window.location.replace('index.html'...)`. Keep the existing CSP meta tag and title structure. Also add `window.location.hash` forwarding (currently missing from multi.html).

    5. Update `docs/sw.js`:
       - Change `CACHE_VERSION` from `'custard-v16'` to `'custard-v17'`
       - Remove `'./calendar.html'` and `'./widget.html'` from the STATIC_ASSETS array
       - Do NOT add any redirect stubs to STATIC_ASSETS (caching them has no value)

    6. Update `custard-calendar/tests/test_sw_precache.py`:
       - In `TestCacheVersionBumped.test_cache_version_is_not_v15`, update the assertion to also check the version is not `custard-v16` (or better: check it IS at least `custard-v17`). The test name references v15 but should now gate on v16 too.

    7. Update `custard-calendar/tests/test_sw_registration.py`:
       - Remove `"widget.html"` and `"calendar.html"` from `USER_FACING_PAGES` list (line 12-21)
       - Remove `"widget.html"` and `"calendar.html"` from `INLINE_PAGES` list (line 30)
       - Update the test name `test_all_8_pages_have_sw_registration` to `test_all_6_pages_have_sw_registration` (or just update the number in the docstring)

    8. Run redirect tests -- they should now PASS (GREEN).
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && uv run pytest tests/test_redirects.py tests/test_sw_precache.py tests/test_sw_registration.py -x -v</automated>
  </verify>
  <done>
    - All 6 redirect stubs exist as bare HTML under 1000 bytes each
    - Each stub has correct meta refresh destination and JS fallback with query+hash forwarding
    - multi.html points directly to index.html (no scoop.html hop)
    - sw.js CACHE_VERSION is custard-v17, calendar.html and widget.html removed from STATIC_ASSETS
    - test_sw_registration.py no longer expects widget.html or calendar.html
    - test_sw_precache.py gates on v17+
    - All new and updated Python tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update browser tests for redirect stub conversion</name>
  <files>
    custard-calendar/worker/test/browser/nav-clickthrough.spec.mjs,
    custard-calendar/worker/test/browser/scoop-compat.spec.mjs,
    custard-calendar/worker/test/browser/primary-store-persistence.spec.mjs,
    custard-calendar/worker/test/browser/shared-nav-store.spec.mjs,
    custard-calendar/worker/test/browser/drive-preferences.spec.mjs,
    custard-calendar/worker/test/browser/alerts-telemetry.spec.mjs,
    custard-calendar/worker/test/browser/radar-phase2.spec.mjs,
    custard-calendar/worker/test/browser/index-drive-defaults.spec.mjs
  </files>
  <action>
    Read each of these 8 browser test files to understand what they test, then apply the appropriate fix strategy per test:

    **Strategy A -- Remove page from test list** (page was just in a navigation list, not the test subject):
    - `nav-clickthrough.spec.mjs`: Remove `/calendar.html` and `/scoop.html` from the `ALL_PAGES` array. These pages no longer have `#shared-nav` since they are redirect stubs.
    - `primary-store-persistence.spec.mjs`: Remove any `goto` calls to `/alerts.html`, `/siri.html`, `/radar.html` since these are now redirect stubs. If the test visits these pages to verify store persistence across navigation, retarget those visits to pages that still exist (e.g., `/updates.html` for alerts/siri, `/index.html` for radar).
    - `shared-nav-store.spec.mjs`: Remove or retarget any `/calendar.html` visit to `/updates.html`.
    - `drive-preferences.spec.mjs`: Remove or retarget any `/calendar.html` visit to `/updates.html`.

    **Strategy B -- Retarget to destination page** (test verifies behavior that now lives on the destination page):
    - `scoop-compat.spec.mjs`: This tests that visiting `/scoop.html?stores=...` loads Today's Drive content. Since scoop.html now redirects to index.html, this compat test should be either: (a) rewritten to verify the redirect itself (visit scoop.html, assert URL changes to index.html), or (b) deleted if the redirect test in test_redirects.py already covers it. Prefer (a) -- rewrite to verify redirect behavior with Playwright `waitForURL`.
    - `index-drive-defaults.spec.mjs`: If this visits `/scoop.html?stores=...`, retarget to `/index.html?stores=...` since that is the destination now.

    **Strategy C -- Delete or skip** (test is for features that no longer exist on this page):
    - `alerts-telemetry.spec.mjs`: If this tests alert signup behavior on alerts.html, the signup form now lives on updates.html. Either retarget to updates.html or skip if redundant with updates-page.spec.mjs.
    - `radar-phase2.spec.mjs`: This has 4 test cases for Radar interactions on radar.html. Radar features now live on index.html (the Today page shows forecast data). These tests should be skipped with a descriptive comment: `// Skipped: radar.html is now a redirect stub to index.html. Radar features live on Today page.` Or delete if the features were already covered by today-page tests.

    **Important:** Read each file first to understand what it actually does before applying the strategy. The strategies above are based on the RESEARCH.md analysis but the actual file contents determine the correct fix.

    After all updates, run the full browser test suite to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npm run test:browser -- --workers=1</automated>
  </verify>
  <done>
    - All 8 browser test files updated to account for redirect stub conversion
    - No browser test visits a redirect stub expecting full page content
    - Full browser test suite passes with zero failures
    - No test references removed pages without appropriate skip/retarget comment
  </done>
</task>

</tasks>

<verification>
1. Python static tests pass: `cd custard-calendar && uv run pytest tests/test_redirects.py tests/test_sw_precache.py tests/test_sw_registration.py -x -v`
2. Browser tests pass: `cd custard-calendar/worker && npm run test:browser -- --workers=1`
3. Each redirect stub is under 1000 bytes: `wc -c docs/scoop.html docs/radar.html docs/calendar.html docs/widget.html docs/siri.html docs/alerts.html`
4. No redirect stub loads JS stack: `grep -l 'planner-shared\|shared-nav\|style\.css' docs/scoop.html docs/radar.html docs/calendar.html docs/widget.html docs/siri.html docs/alerts.html` returns nothing
</verification>

<success_criteria>
- 6 redirect stubs replace full pages, each under 1000 bytes
- Query params and hash fragments forwarded via JS fallback
- multi.html skips the scoop.html hop, redirects directly to index.html
- SW CACHE_VERSION is custard-v17, calendar.html and widget.html removed from STATIC_ASSETS
- All Python and browser tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-redirects-css-cleanup/10-01-SUMMARY.md`
</output>
