---
phase: 36-data-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - worker/test/quality-gate.test.js
  - worker/test/reconciliation.test.js
autonomous: true
requirements:
  - DATA-02
  - DATA-03
must_haves:
  truths:
    - "Quality gate tests cover unknown flavors, duplicate same-day, and stale data detection"
    - "Reconciliation tests independently compute avg_gap_days, rarity labels, and last_seen from raw snapshot rows and assert parity with production logic"
    - "All new tests initially fail (RED phase) because production code does not yet implement the new patterns"
  artifacts:
    - path: "worker/test/quality-gate.test.js"
      provides: "Tests for 3 new detection patterns (D-05a unknown flavors, D-05b duplicate same-day, D-05c stale data)"
      min_lines: 80
    - path: "worker/test/reconciliation.test.js"
      provides: "Stats reconciliation: avg_gap_days, rarity label derivation, last_seen, overdue calculations"
      min_lines: 100
  key_links:
    - from: "worker/test/quality-gate.test.js"
      to: "worker/src/kv-cache.js"
      via: "imports sanitizeFlavorPayload and new detection functions"
      pattern: "import.*kv-cache"
    - from: "worker/test/reconciliation.test.js"
      to: "worker/src/route-today.js"
      via: "tests the same rarity label derivation logic (3-gate system)"
      pattern: "avgGapDays|avg_gap_days|Ultra Rare|Rare"
---

<objective>
Create the test infrastructure for Phase 36's quality gate and stats reconciliation work. These tests define the expected behavior of the new detection patterns (D-05) and the reconciliation suite (D-09) BEFORE production code is written, establishing a TDD-style RED phase.

Purpose: Tests-first ensures Plan 02's implementation has clear pass/fail targets. The reconciliation suite becomes a permanent regression test preventing rarity/stats drift.
Output: Two new test files that will turn GREEN in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-data-quality/36-CONTEXT.md
@.planning/phases/36-data-quality/36-RESEARCH.md

@worker/src/kv-cache.js
@worker/src/flavor-stats.js
@worker/src/route-today.js
@worker/src/flavor-matcher.js
@worker/src/flavor-colors.js
@worker/src/operator-alerts.js
@worker/test/flavor-stats.test.js

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From worker/src/kv-cache.js:
```javascript
export const CLOSED_TITLE_RE = /\bclosed\b|^z[_ ]*(store|restaurant)?closed/i;

export function sanitizeFlavorPayload(payload) {
  // Returns: { data: { name, flavors: [{date, title, description}] }, dropped, rawCount }
}
```

From worker/src/flavor-matcher.js:
```javascript
export function normalize(name) {
  // Strips trademark symbols, lowercases, collapses whitespace
}
```

From worker/src/flavor-colors.js:
```javascript
export const FLAVOR_PROFILES = { /* ~80 entries, keyed by normalized flavor name */ };
export const FLAVOR_ALIASES = { /* ~40 entries, maps alternate names to canonical */ };
```

From worker/src/route-today.js (lines 124-152) -- rarity 3-gate system:
```javascript
// Gate 1: data quality -- need at least 10 appearances AND 90+ day span
const meetsDataQuality = appearances >= 10 && spanDays >= 90;
// Gate 2: network-wide -- suppress if served at >100 stores in last 30 days
const meetsNetworkGate = networkStoreCount <= 100;
// Gate 3: derive rarity label from avg_gap_days
// Ultra Rare: >150 days; Rare: 90-150 days; null otherwise
```

From worker/test/flavor-stats.test.js:
```javascript
function createMockD1(rows = []) {
  // In-memory D1 mock routing SQL queries to array filters
  // Proven pattern with 15+ tests
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Quality gate detection tests (D-05a/b/c)</name>
  <files>worker/test/quality-gate.test.js</files>
  <read_first>
    - worker/src/kv-cache.js (current sanitizeFlavorPayload implementation)
    - worker/src/flavor-colors.js (FLAVOR_PROFILES keys, FLAVOR_ALIASES keys -- scan exports)
    - worker/src/flavor-matcher.js (normalize function signature)
    - worker/test/kv-cache.test.js (existing test patterns for sanitize)
  </read_first>
  <action>
Create worker/test/quality-gate.test.js with three describe blocks testing detection functions that will be exported from kv-cache.js in Plan 02:

**D-05a: Unknown flavor detection.** Import `isKnownFlavor` (to be exported from kv-cache.js). Test cases:
- `isKnownFlavor('Butter Pecan')` returns true (in FLAVOR_PROFILES as 'butter pecan')
- `isKnownFlavor('Chocolate Volcano')` returns true (in FLAVOR_PROFILES as 'chocolate volcano')
- `isKnownFlavor('Totally Made Up Flavor 9000')` returns false
- `isKnownFlavor('Andes Mint Avalanche')` returns true (handles trademark stripping)
- `isKnownFlavor('')` returns false
- `isKnownFlavor(null)` returns false
- Test an alias: `isKnownFlavor('Cookie Dough')` returns true (FLAVOR_ALIASES maps 'cookie dough' -> a canonical name)

**D-05b: Duplicate same-day detection.** Import `detectDuplicateDays` (to be exported from kv-cache.js). This function accepts an array of `{date, title}` objects and returns an array of duplicate date strings. Test cases:
- No duplicates: `[{date:'2026-04-01',title:'A'},{date:'2026-04-02',title:'B'}]` returns `[]`
- One duplicate: `[{date:'2026-04-01',title:'A'},{date:'2026-04-01',title:'B'}]` returns `['2026-04-01']`
- Multiple duplicates across different dates
- Empty array returns `[]`

**D-05c: Stale data detection.** Import `isStaleStore` (to be exported from kv-cache.js). This function accepts `(lastFlavorDate, now, thresholdDays=7)` and returns boolean. Test cases:
- 3 days ago: not stale (threshold 7)
- 8 days ago: stale (threshold 7)
- Exactly 7 days ago: not stale (boundary: <=7 is OK)
- null lastFlavorDate: returns true (no data = stale)
- Custom threshold of 3 days works

All tests should import from `../src/kv-cache.js`. Since the functions don't exist yet, these tests will FAIL when run -- this is the expected RED state. Mark the test file with a comment at the top: `// RED phase: these tests target functions to be implemented in Plan 02`.

Use vitest `describe/it/expect` pattern matching existing test files.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx vitest run test/quality-gate.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - worker/test/quality-gate.test.js exists and contains `import { isKnownFlavor, detectDuplicateDays, isStaleStore } from '../src/kv-cache.js'`
    - File contains `describe('D-05a: unknown flavor detection'`
    - File contains `describe('D-05b: duplicate same-day detection'`
    - File contains `describe('D-05c: stale data detection'`
    - File contains at least 12 `it(` test cases total
    - Tests fail with import errors (functions not yet exported) -- this is expected RED state
    - Comment at top: `// RED phase: these tests target functions to be implemented in Plan 02`
  </acceptance_criteria>
  <done>Quality gate test file exists with 12+ test cases covering all three D-05 detection patterns. Tests fail because the target functions are not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Stats reconciliation test suite (D-09)</name>
  <files>worker/test/reconciliation.test.js</files>
  <read_first>
    - worker/src/route-today.js (lines 120-155 -- the 3-gate rarity logic, avg_gap_days computation)
    - worker/src/flavor-stats.js (buildSingleFlavorStats -- independent stats computation)
    - worker/test/flavor-stats.test.js (createMockD1 pattern -- reuse this)
  </read_first>
  <action>
Create worker/test/reconciliation.test.js that independently computes stats from raw D1 snapshot rows and asserts they match the production computation logic. Per D-09, this is a permanent regression prevention suite.

**Key insight from RESEARCH.md Pitfall 1:** Rarity label logic lives in route-today.js (3-gate system) and flavor-stats.js (raw stats only, no labels). The reconciliation test must verify BOTH: (a) raw stat computation (avg_gap_days, last_seen, days_since_last) and (b) rarity label derivation from those stats (the 3-gate system from route-today.js lines 124-152).

**Extract and test the rarity label derivation function.** Create a pure function `deriveRarityLabel({ appearances, spanDays, avgGapDays, networkStoreCount })` in the test file that mirrors route-today.js lines 124-152:
- Gate 1: appearances >= 10 AND spanDays >= 90
- Gate 2: networkStoreCount <= 100
- Gate 3: avgGapDays > 150 = 'Ultra Rare', avgGapDays > 90 = 'Rare', else null

Test cases for `deriveRarityLabel`:
1. Ultra Rare: 12 appearances, 400-day span, avgGapDays=160, networkStoreCount=50 -> 'Ultra Rare'
2. Rare: 15 appearances, 200-day span, avgGapDays=100, networkStoreCount=30 -> 'Rare'
3. Common (not rare): 20 appearances, 300-day span, avgGapDays=15, networkStoreCount=20 -> null
4. Fails Gate 1 (too few appearances): 5 appearances, 200-day span, avgGapDays=120 -> null
5. Fails Gate 1 (span too short): 15 appearances, 60-day span, avgGapDays=4 -> null
6. Fails Gate 2 (too many stores): 15 appearances, 200-day span, avgGapDays=120, networkStoreCount=150 -> null
7. Boundary: avgGapDays=90 exactly -> null (Rare requires > 90, not >=)
8. Boundary: avgGapDays=150 exactly -> 'Rare' (Ultra Rare requires > 150, not >=)

Test cases for independent avg_gap_days computation:
1. Simple: dates ['2025-01-01', '2025-02-01', '2025-03-01'] -> avgGapDays = Math.round((31+28)/2) = 30
2. Single date: ['2025-06-01'] -> avgGapDays = null
3. Two dates: ['2025-01-01', '2025-07-01'] -> avgGapDays = 181
4. Irregular spacing: ['2025-01-01', '2025-01-10', '2025-06-01'] -> Math.round((9+142)/2) = 76

Test cases for last_seen and days_since_last:
1. Most recent date is '2026-03-01', "now" is '2026-04-05' -> days_since_last = 35, last_seen = '2026-03-01'
2. No dates -> last_seen = null, days_since_last = null

Test cases for overdue detection (per flavor-stats.js pattern):
1. avgGapDays=30, days_since_last=45 -> overdue (days_since > 1.5x avgGap)
2. avgGapDays=30, days_since_last=20 -> not overdue

NOTE: The deriveRarityLabel function is extracted into the test file as a reference implementation. In a future plan, this should be extracted to a shared module -- but per D-10, we do NOT modify production rarity logic in this phase. The test validates that the independent computation matches what route-today.js would produce.

Use vitest describe/it/expect. Import nothing from production code in this test -- the reconciliation suite is fully independent with its own computation functions.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar/worker && npx vitest run test/reconciliation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - worker/test/reconciliation.test.js exists
    - File contains `function deriveRarityLabel(` implementing the 3-gate system
    - File contains `describe('rarity label derivation (3-gate system)'`
    - File contains `describe('independent avg_gap_days computation'`
    - File contains `describe('last_seen and days_since_last'`
    - File contains `describe('overdue detection'`
    - Contains at least 14 `it(` test cases
    - All tests PASS (these are self-contained computations, no external imports needed)
    - Gate thresholds match route-today.js: appearances >= 10, spanDays >= 90, networkStoreCount <= 100, Ultra Rare > 150, Rare > 90
  </acceptance_criteria>
  <done>Reconciliation test suite passes with 14+ tests covering avg_gap_days, rarity labels, last_seen, and overdue -- providing permanent regression prevention per D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan creates test files only -- no production code changes, no data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Information Disclosure | test/reconciliation.test.js | accept | Test file contains only synthetic data and publicly known rarity thresholds; no secrets |
</threat_model>

<verification>
- `cd worker && npx vitest run test/quality-gate.test.js` -- expected to FAIL (RED phase, functions not yet exported)
- `cd worker && npx vitest run test/reconciliation.test.js` -- expected to PASS (self-contained independent computations)
- `cd worker && npm test` -- existing 1149+ tests still pass (new test files do not break existing suite)
</verification>

<success_criteria>
- quality-gate.test.js exists with 12+ tests covering D-05a/b/c detection patterns
- reconciliation.test.js exists with 14+ tests covering D-09 stats reconciliation
- Reconciliation tests pass independently
- Quality gate tests fail only because target functions are not yet implemented (not due to test bugs)
- Existing test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/36-data-quality/36-01-SUMMARY.md`
</output>
