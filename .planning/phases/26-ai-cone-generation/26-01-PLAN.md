---
phase: 26-ai-cone-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/generate_masterlock_prompts.mjs
  - docs/assets/masterlock-flavor-fills.json
  - docs/assets/masterlock-flavor-fills.js
  - docs/assets/masterlock-prompt-pack.md
  - scripts/verify-cone-assets.mjs
autonomous: true
requirements: [GEN-02]

must_haves:
  truths:
    - "All 94 flavors have premium treatment overrides with base, swirls, chunks, texture fields"
    - "All 94 flavors have descriptions (40 from flavors.json + 54 from DESCRIPTION_FALLBACK)"
    - "generate_masterlock_prompts.mjs runs without errors and produces 94 flavor fill cards"
    - "A verification script can check cone asset counts, dimensions, and manifest completeness"
  artifacts:
    - path: "tools/generate_masterlock_prompts.mjs"
      provides: "93 premium treatment overrides + 54 description fallbacks"
      contains: "PREMIUM_TREATMENT_OVERRIDES"
    - path: "docs/assets/masterlock-flavor-fills.json"
      provides: "Complete prompt data for all 94 flavors"
    - path: "scripts/verify-cone-assets.mjs"
      provides: "Verification script for cone assets, dimensions, and manifest"
      exports: "CLI script"
  key_links:
    - from: "tools/generate_masterlock_prompts.mjs"
      to: "docs/assets/masterlock-flavor-fills.json"
      via: "script generates JSON output"
      pattern: "masterlock-flavor-fills\\.json"
---

<objective>
Author 93 premium treatment overrides and 54 missing flavor descriptions in the prompt generation script, then regenerate the complete flavor fills data for all 94 flavors. Also create the verification script that downstream plans and the final phase gate will use.

Purpose: The generation pipeline (Plan 02) needs complete, high-quality prompt data for every flavor before it can generate images. Currently only 1/94 has a premium override and only 40/94 have descriptions.
Output: Updated `generate_masterlock_prompts.mjs` with all 94 overrides + descriptions, regenerated `masterlock-flavor-fills.json` with 94 entries (all with premium overrides), and `scripts/verify-cone-assets.mjs` verification script.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-ai-cone-generation/26-CONTEXT.md
@.planning/phases/26-ai-cone-generation/26-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From tools/generate_masterlock_prompts.mjs:
- PREMIUM_TREATMENT_OVERRIDES: object keyed by flavor key string, value is { base: string, swirls: string, chunks: string, texture: string }
- DESCRIPTION_FALLBACK: object keyed by flavor key string, value is description string
- Currently: 1 premium override (blackberry cobbler), 2 description fallbacks (double butter pecan, oreo cookie overload)
- Target: 94 premium overrides, 54+ description fallbacks (enough for all 94 flavors to resolve)

From worker/src/flavor-colors.js:
- FLAVOR_PROFILES: 94 entries, each with { base, ribbon?, toppings?:[], density? }
- BASE_COLORS: { vanilla: '#F5DEB3', chocolate: '#6F4E37', ... } (13 entries)
- RIBBON_COLORS: { caramel: '#D38B2C', ... } (5 entries)
- TOPPING_COLORS: { oreo: '#1A1A1A', ... } (20 entries)

From docs/flavors.json:
- 40 flavor entries with { title, description } -- the other 54 must come from DESCRIPTION_FALLBACK

From docs/assets/blackberry-cobbler/blackberry-prompts.md:
- Reference L5 premium prompt template (the exact format that produces good results)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 93 premium treatment overrides and 54 description fallbacks</name>
  <files>tools/generate_masterlock_prompts.mjs</files>
  <read_first>
    - tools/generate_masterlock_prompts.mjs (current state with 1 override, 2 fallbacks)
    - worker/src/flavor-colors.js (FLAVOR_PROFILES -- all 94 entries with base/ribbon/toppings/density)
    - docs/assets/blackberry-cobbler/blackberry-prompts.md (reference L5 premium prompt quality)
    - docs/flavors.json (40 existing descriptions -- identify which 54 are missing)
  </read_first>
  <action>
Update `tools/generate_masterlock_prompts.mjs` with two additions:

**A) Expand DESCRIPTION_FALLBACK** to cover the 54 flavor keys NOT in `docs/flavors.json`. The 40 flavors already in flavors.json do NOT need fallback entries. For each missing flavor, write a 1-sentence Culver's-style description derived from the FLAVOR_PROFILES data. Pattern: "{Base} Fresh Frozen Custard with {toppings and ribbon description}." Examples:
- `'andes mint avalanche': 'Mint Fresh Frozen Custard packed with Andes mint pieces and dark chocolate chunks.'`
- `'caramel turtle': 'Caramel Fresh Frozen Custard with fudge, pecans, and caramel.'`
- `'grasshopper fudge': 'Mint Fresh Frozen Custard swirled with fudge and loaded with OREO cookie pieces.'`

To determine which 54 are missing: load flavors.json, normalize each title to a key using `normalizeName()`, then check which FLAVOR_PROFILES keys are NOT matched. Write DESCRIPTION_FALLBACK entries for exactly those missing keys.

**B) Expand PREMIUM_TREATMENT_OVERRIDES** from 1 entry (blackberry cobbler) to all 94 entries. Each override is an object with four string fields: `base`, `swirls`, `chunks`, `texture`.

The premium override for each flavor must be richer and more visually descriptive than the canonical auto-generated treatment. Use these patterns derived from the blackberry cobbler reference:

- **base**: Describe the custard color with marbling or visual qualities. Include hex color reference(s). Example: `'Rich chocolate custard with warm cocoa depth (#6F4E37 with darker #5A3825 undertones).'`
- **swirls**: Describe ribbon/swirl visual appearance, or `'None -- pure base with {density description}.'` if no ribbon. Example: `'Golden caramel ribbons with glossy amber sheen winding through the scoop (#D38B2C).'`
- **chunks**: Describe each topping with visual detail (shape, color, texture, sheen). Separate multiple toppings with semicolons. Example: `'Dark OREO cookie chunks with visible cream filling layers (#1A1A1A with cream streaks); bright Andes mint pieces with green-white striping (#1FAE7A).'`
- **texture**: Describe overall visual density and depth. Reference the DENSITY_NOTES for the flavor's density level but make it more specific. Example: `'Dense inclusion field with layered depth. Each ingredient clearly readable against mint base. Andes pieces catch light differently than OREO chunks.'`

For flavors with `density: 'pure'` (no toppings, no ribbon), the premium override should emphasize the custard body itself -- color gradients, highlight reflections, creamy texture, subtle color variation within the scoop.

Do NOT modify the MASTERLOCK_TEMPLATE, QUALITY_LADDER, or any function logic -- only the PREMIUM_TREATMENT_OVERRIDES and DESCRIPTION_FALLBACK data objects.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && node tools/generate_masterlock_prompts.mjs && node -e "const j=JSON.parse(require('fs').readFileSync('docs/assets/masterlock-flavor-fills.json','utf8')); const n=j.flavors.length; const p=j.flavors.filter(f=>f.premium_treatment_override).length; console.log('Flavors:',n,'Premium:',p); if(n!==94||p!==94){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - tools/generate_masterlock_prompts.mjs contains exactly 94 keys in PREMIUM_TREATMENT_OVERRIDES (grep: every key from FLAVOR_PROFILES appears)
    - tools/generate_masterlock_prompts.mjs DESCRIPTION_FALLBACK has at least 54 entries (enough for the 54 flavors not in flavors.json)
    - `node tools/generate_masterlock_prompts.mjs` exits 0 with no errors
    - docs/assets/masterlock-flavor-fills.json contains 94 flavor entries (grep: `"flavor_key"` appears 94 times)
    - All 94 entries in masterlock-flavor-fills.json have non-null `premium_treatment_override` (grep: `"premium_treatment_override": null` returns 0 matches)
    - Each premium_treatment_override object has all four fields: base, swirls, chunks, texture
  </acceptance_criteria>
  <done>All 94 flavors have premium treatment overrides and descriptions. The prompt generation script runs without errors and produces masterlock-flavor-fills.json with 94 complete entries.</done>
</task>

<task type="auto">
  <name>Task 2: Create verification script for cone assets</name>
  <files>scripts/verify-cone-assets.mjs</files>
  <read_first>
    - scripts/generate-hero-cones.mjs (reference for sharp metadata reading pattern, flavorSlug function)
    - worker/src/flavor-colors.js (FLAVOR_PROFILES keys -- the 94 slugs to check)
    - docs/assets/blackberry-cobbler/blackberry-asset-manifest.json (reference manifest structure)
  </read_first>
  <action>
Create `scripts/verify-cone-assets.mjs` -- a Node.js CLI verification script that checks cone asset completeness. It will be used by Plans 02 and 03 and the final phase gate.

The script must import sharp (resolve from worker/node_modules using the same pattern as generate-hero-cones.mjs) and FLAVOR_PROFILES from worker/src/flavor-colors.js.

**Slug function:** Use the same `flavorSlug()` from generate-hero-cones.mjs:
```javascript
function flavorSlug(name) {
  return name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
}
```

**CLI modes (mutually exclusive flags):**

1. `node scripts/verify-cone-assets.mjs` (no flags) -- Check that all 94 PNGs exist at `docs/assets/cones/{slug}.png`. Report count and list any missing slugs. Exit 0 if 94/94 present, exit 1 otherwise.

2. `node scripts/verify-cone-assets.mjs --dimensions` -- For each PNG in docs/assets/cones/, use `sharp(path).metadata()` to verify `width` and `height`. Accept either 288x336 or 144x168. Report any files with unexpected dimensions. Exit 0 if all match, exit 1 if any mismatch.

3. `node scripts/verify-cone-assets.mjs --manifest` -- Check that `docs/assets/ai-generation-manifest.json` exists and has entries for all 94 flavors. Each entry must have: `flavor_key`, `slug`, `model`, `quality`, `prompt_hash`, `candidates` (array with >=1 entry), `selected` (non-null string), `status` (must be "approved"). Report any incomplete entries. Exit 0 if all valid, exit 1 otherwise.

4. `node scripts/verify-cone-assets.mjs --transparency` -- For each PNG in docs/assets/cones/, read with sharp and check that it has an alpha channel (`metadata().channels === 4` and `metadata().hasAlpha === true`). Report any files without transparency. Exit 0 if all have alpha, exit 1 otherwise.

5. `node scripts/verify-cone-assets.mjs --all` -- Run all four checks in sequence, report combined results.

**Output format:** Each check prints a header line (`=== Check: {name} ===`), result summary (`PASS: 94/94 PNGs present` or `FAIL: 91/94 PNGs present, missing: [slug1, slug2, slug3]`), and sets exit code.

Parse CLI flags using `process.argv.includes('--flag')` -- no dependency needed.
  </action>
  <verify>
    <automated>cd /Users/chriskaschner/Documents/GitHub/custard/custard-calendar && node scripts/verify-cone-assets.mjs 2>&1 | head -5 && echo "Script runs"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-cone-assets.mjs exists and is executable via `node scripts/verify-cone-assets.mjs`
    - Script imports FLAVOR_PROFILES from worker/src/flavor-colors.js
    - Script contains flavorSlug function matching `name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '')`
    - Script supports --dimensions, --manifest, --transparency, --all flags (grep: `process.argv.includes`)
    - Running without flags outputs `=== Check:` header and a count like `94` or `PASS` or `FAIL`
    - Script uses sharp for metadata reads (grep: `sharp(` appears in the file)
  </acceptance_criteria>
  <done>Verification script exists, runs without errors, and checks file counts, dimensions, manifest, and transparency. It will be used in Plans 02 and 03 for automated verification.</done>
</task>

</tasks>

<verification>
- `node tools/generate_masterlock_prompts.mjs` exits 0 and produces 94 entries in masterlock-flavor-fills.json
- All 94 entries have non-null premium_treatment_override with base/swirls/chunks/texture fields
- `node scripts/verify-cone-assets.mjs` runs without crashing (will report 94/94 existing PNGs since SVG-rasterized ones exist, but --manifest will fail since manifest doesn't exist yet -- that's expected)
- `cd worker && npm test` passes (no regressions)
</verification>

<success_criteria>
- 94 premium treatment overrides authored in generate_masterlock_prompts.mjs
- 54+ description fallbacks authored for flavors not in flavors.json
- masterlock-flavor-fills.json regenerated with 94 complete entries (all with premium overrides)
- verify-cone-assets.mjs created with 4 check modes + --all
- No changes to MASTERLOCK_TEMPLATE or function logic
</success_criteria>

<output>
After completion, create `.planning/phases/26-ai-cone-generation/26-01-SUMMARY.md`
</output>
