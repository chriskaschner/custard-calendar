---
phase: 33.1-bug-fixes-store-disambiguation-rarity-logic-art-migration-stale-signals
plan: 01
subsystem: ui
tags: [css, truncation, rarity, localStorage, display-name, store-disambiguation]

requires:
  - phase: 33-performance
    provides: "localStorage hero cache and SW API caching infrastructure"
provides:
  - "CSS chip truncation for disambiguated store names (180px/140px)"
  - "Uncommon rarity badge suppression via display:none"
  - "Hero cache displayName field for human-readable cached render"
affects: [33.1-02, compare-page, today-page]

tech-stack:
  added: []
  patterns:
    - "displayName cached in localStorage alongside slug for offline-friendly human-readable names"
    - "Fallback chain: cached displayName -> title-cased slug for backward compatibility"

key-files:
  created: []
  modified:
    - docs/style.css
    - docs/today-page.js

key-decisions:
  - "Uncommon rarity badge suppressed via CSS display:none (tokens retained for backward compat)"
  - "Hero cache writes displayName from CustardPlanner.getDisplayName() at cache time, with title-cased slug fallback for old caches"

patterns-established:
  - "Cache enrichment: compute derived display values at write time, not read time"

requirements-completed: [BUG-01, BUG-02, BUG-03]

duration: 2min
completed: 2026-03-26
---

# Phase 33.1 Plan 01: CSS chip truncation, uncommon badge suppression, and hero cache display name Summary

**Compare store chips and flavor row labels truncate disambiguated names with ellipsis; Uncommon rarity badge hidden; hero cached render shows human-readable store name instead of raw slug**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-26T17:35:23Z
- **Completed:** 2026-03-26T17:37:19Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Compare store chip names (.compare-store-chip-name) truncate at 180px with CSS text-overflow: ellipsis
- Compare flavor row store labels (.compare-store-label) truncate at 140px with ellipsis
- Uncommon rarity badge hidden via display: none, aligning with Gate 3 rarity overhaul
- Hero card cached render uses displayName from CustardPlanner.getDisplayName() with title-cased slug fallback for backward compatibility

## Task Commits

Each task was committed atomically:

1. **Task 1: CSS fixes -- chip truncation and uncommon badge suppression** - `0b1042a` (fix)
2. **Task 2: Hero card cached render uses display name instead of raw slug** - `1dc6fd0` (fix)

## Files Created/Modified
- `docs/style.css` - Added max-width/ellipsis truncation to .compare-store-chip-name (180px) and .compare-store-label (140px); changed .rarity-badge-uncommon to display: none
- `docs/today-page.js` - Added displayName parameter to cacheHeroData(); computed display name at cache write site; cached render uses heroCache.displayName with title-cased slug fallback

## Decisions Made
- Uncommon rarity badge suppressed via CSS (display: none) rather than removing the class entirely -- rarity color tokens stay in :root for backward compatibility
- displayName computed at cache write time (not read time) so cached renders are instant without needing to wait for store manifest load
- Title-cased slug fallback ensures old caches without displayName field still show a reasonable name

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Plan 01 CSS and JS fixes are complete; plan 02 can proceed with remaining bug fixes (rarity logic, art migration, insight signals)
- The displayName caching pattern established here can be reused if other surfaces need cached human-readable names

## Known Stubs
None - all changes are functional with no placeholder data.

## Self-Check: PASSED

---
*Phase: 33.1-bug-fixes-store-disambiguation-rarity-logic-art-migration-stale-signals*
*Completed: 2026-03-26*
