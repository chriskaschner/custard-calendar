---
phase: 17-png-generation-deployment
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - docs/sw.js
autonomous: false
requirements: [PNGS-02]

must_haves:
  truths:
    - "CACHE_VERSION in docs/sw.js is 'custard-v19'"
    - "Returning users will get fresh PNGs after service worker activates new cache"
    - "Today page shows consistent Hero PNG rendering for all profiled flavors"
  artifacts:
    - path: "docs/sw.js"
      provides: "Updated CACHE_VERSION = 'custard-v19'"
      contains: "custard-v19"
  key_links:
    - from: "docs/sw.js activate handler"
      to: "CACHE_VERSION"
      via: "Old caches purged when name !== CACHE_VERSION"
      pattern: "custard-v19"
---

<objective>
Bump service worker CACHE_VERSION to custard-v19 so returning users pick up fresh Hero cone PNGs. Visual checkpoint confirms consistent Hero-tier rendering on the Today page.

Purpose: Without the cache bump, returning users would continue seeing stale pre-Phase-13 PNGs from the old custard-v18 cache. The bump triggers cache purge on next visit.

Output: Updated sw.js with custard-v19, visual verification of Today page rendering.
</objective>

<execution_context>
@/Users/chriskaschner/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chriskaschner/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-png-generation-deployment/17-CONTEXT.md

@docs/sw.js

<interfaces>
<!-- From docs/sw.js line 1 -->
```javascript
const CACHE_VERSION = 'custard-v18';
```

<!-- From Plan 01 output: worker/test/png-asset-count.test.js includes a CACHE_VERSION assertion expecting 'custard-v19' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump CACHE_VERSION to custard-v19</name>
  <files>docs/sw.js</files>
  <action>
1. Edit `docs/sw.js` line 1: change `const CACHE_VERSION = 'custard-v18';` to `const CACHE_VERSION = 'custard-v19';`.
   This is the ONLY change to sw.js. Do not modify STATIC_ASSETS, fetch handlers, or anything else.

2. Run the CACHE_VERSION CI test from Plan 01:
   `cd custard-calendar/worker && npx vitest run png-asset-count.test.js -t "CACHE_VERSION"`
   This should now pass (was failing in Plan 01 with v18).

3. Run full test suite:
   `cd custard-calendar/worker && npm test`

Per user decision: "Cache version bump in a separate final commit after PNG generation and verification" and "Bump CACHE_VERSION from custard-v18 to custard-v19 in docs/sw.js".
  </action>
  <verify>
    <automated>cd custard-calendar/worker && npx vitest run png-asset-count.test.js && npm test</automated>
  </verify>
  <done>CACHE_VERSION is 'custard-v19' in docs/sw.js. All CI tests pass including PNG count, alias structure, cache version, golden baselines, and full suite.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Hero PNG rendering</name>
  <files>docs/flavor-audit.html, docs/index.html</files>
  <action>
User visually verifies that all 94 profiled flavors render as Hero PNGs and aliased flavors resolve correctly. This checkpoint confirms the entire Phase 17 deliverable.
  </action>
  <what-built>All 94 Hero cone PNGs regenerated from updated FLAVOR_PROFILES, heroConeSrc() alias resolution added, service worker cache version bumped to custard-v19. Every profiled flavor now has a Hero-tier PNG and aliased names resolve correctly.</what-built>
  <how-to-verify>
    1. Open `custard-calendar/docs/flavor-audit.html` in a browser to visually review cone rendering across all 94 profiled flavors
    2. Spot-check a few aliased flavors by searching for their alias names in the audit page
    3. Open `custard-calendar/docs/index.html` (Today page) in a browser to verify Hero PNG rendering is consistent -- no mixing of PNG and SVG quality levels for profiled flavors
    4. Check that unknown/unprofiled flavors (if any appear) gracefully fall back to HD SVG rendering rather than showing broken images
  </how-to-verify>
  <verify>Human visual approval</verify>
  <done>User confirms consistent Hero-tier PNG rendering across the site with no broken images or quality mixing.</done>
  <resume-signal>Type "approved" or describe any issues with cone rendering quality</resume-signal>
</task>

</tasks>

<verification>
1. `grep 'CACHE_VERSION' custard-calendar/docs/sw.js` shows 'custard-v19'
2. `cd custard-calendar/worker && npx vitest run png-asset-count.test.js` -- all three test groups pass (PNG count, alias structure, cache version)
3. `cd custard-calendar/worker && npm test` -- full suite green
4. Human visual verification of Today page and flavor-audit.html confirms consistent Hero PNG rendering
</verification>

<success_criteria>
- CACHE_VERSION is 'custard-v19' in docs/sw.js
- All CI tests pass (PNG count, alias structure, cache version, golden baselines, full suite)
- User visually confirms consistent Hero-tier rendering on Today page
- No broken image fallbacks for profiled or aliased flavors
</success_criteria>

<output>
After completion, create `.planning/phases/17-png-generation-deployment/17-02-SUMMARY.md`
</output>
